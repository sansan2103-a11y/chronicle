# Chronicle 引き継ぎ — 2026-07-26 / fix576 まで

本番: **20260726-fix576**（GitHub Pages 反映済み・実機検証済み）
テスト: **26ファイル / 1029件 / 失敗0**（`node run_all_tests.cjs` が正本）

---

## 1. このセッションで出荷したもの

### fix575 — fix399(クラウド取り込み前の控え)の容量回復削除をゲート経由に

- `while (r === 'quota')` を**単発再試行**へ。GPT不変条件「1回の容量回復につき、削除は最大1論理単位／書込み再試行も最大1回」。
- 候補選択は現行ロジックのまま。**物理削除だけ**を `__v292Dfix569.tryDeleteExact()` へ通す。
- `protected / stale / missing / policy-unavailable / delete-failed` は**別候補へ進まず中止**（fail-closed）。
- ゲートの `ok` を鵜呑みにせず、呼び出し元でも read-back する。
- 実装中に別バグを発見・修正: **中止理由の記録が localStorage にしか無く、容量満杯だと記録自体が失敗**していた。メモリ側を正本に (`__v292Dfix399x.bkLog()`)。

### fix576 — フォールバック契約の修正 ＋ A2(fix490Quota)

- **「ゲート未搭載 → 旧削除経路へ自動フォールバック」を廃止**（GPT指摘）。
  中央保護がロードできなかったことを理由に、いちばん危険な旧経路へ自動で戻るのは不変条件と矛盾する。
  - `v292Dfix575Off === '1'` → 明示的な緊急ロールバックのみ旧経路。`rollbackModeUsed` を記録
  - OFFでないのに `tryDeleteExact` が無い → `policy-unavailable` として**中止**
- **A2**: `fix490` の `dropOldestGuardBackup()` も同じ単発ゲート方式へ。
  緊急停止 `v292Dfix576Off='1'` / `stats().backupSkipped` / `dropLog()`（メモリ正本）。
  失敗の意味は「新しいguard控えを作れなかった」だけで、**本体セーブは巻き戻さない**（fix565の親処理契約を維持）。
- `run_all_tests.cjs` を追加（下記3参照）。

---

## 2. 実機で確認した数字（fix576 / `index.html?story=smrg85jwsn6`）

```
selfTest ok:true / isOutermost:true / loadOrderVerified:true
wrapperErrors:0 / classifierErrors:0 / denied:0
主人公=白石澪 / 実スロット10件健在（+ ab552p1A は診断用の残骸）
cloudsync控え=1件 / guard控え=2件（どちらも「各スロットの最良の控え」=保護対象）
```

**ゲート4分岐を実データで確認済み**（fix575時点）:

| 要求 | 結果 | 実データ |
|---|---|---|
| 通常キー | `deleted` | 実際に消えた |
| bytes不一致 | `stale` | 残った |
| 実在しないキー | `missing` | — |
| **442KBの実cloudsync控え** | **`protected`** | **残った** |

※ protected の検証は、万一消えた場合に即復元できるよう事前に値を控えて実行。復元は不要だった。

---

## 3. テスト母集団の監査（GPT指摘への回答）

**母集団は縮んでいなかった。私の数え方が誤っていた。**

- `git --diff-filter=D` でテストファイルの削除は**0件**。除外も0件。
- 実測（worktreeでfix574を再実行して確認）:
  - fix574 = 24ファイル / 943件
  - fix575 = 25ファイル / 982件
  - fix576 = 26ファイル / 1029件
- 過去の報告「962」は過大計上、「854」はサマリ形式が違う3ファイルの取りこぼし。

→ **その場しのぎの `grep | tail` を廃止し `run_all_tests.cjs` を正本にした**。
  - テストファイルの列挙はここだけ / サマリ4形式に対応
  - ★**サマリを読めなかったファイルはエラー扱い**（0件として素通しさせない＝母集団が無言で縮む唯一の経路を塞ぐ）
  - 配信成果物の sha256(先頭16) も併記

---

## 4. 次にやること — A3（GPT裁定の実装順・そのまま従う）

### 結論（GPT）
> `protectedSet()` は据え置き、新APIを追加し、fix569は影比較を経て `deletePolicy()` へ一本化。
> 生セーブと生サイドストアは `reclaim` では常に拒否し、検証済みのライフサイクル計画だけ exact key を許可する設計で確定。

### 実装順（9段）
1. `fix562.classifyKey()` を**読取専用**で追加
2. `protectedSet()` 不変テスト（既存の返却内容・件数・意味を完全に据え置く）
3. `fix562.deletePolicy()` を追加
4. 生セーブ・サイドストア・snapshot の契約テスト
5. fix569 で**旧判定と新判定を影比較**（まだ切り替えない）
6. 不一致を実データで棚卸し
7. **より厳しい側**でゲート実行
8. 問題がなければ `deletePolicy` を唯一の正本へ
9. 旧 `protectedKeys` を削除判断から外す

### 保護階層（既定動作）

| 種類 | reclaim | retention | lifecycle-delete |
|---|---|---|---|
| `chr6` / `chr6_slot_*`（生セーブ） | 拒否 | 拒否 | 検証済み計画のみ許可 |
| 生きているスロットのサイドストア（12種） | 拒否 | 拒否 | 計画に含まれるもののみ |
| complete snapshot の manifest・実体 | protected | | |
| 各スロットの最良の控え | protected | | |
| 余剰控え | reclaim可能 | | |
| test-fixture | userデータより先にreclaim可能 | | |
| キャッシュ・診断ログ | 条件付きで削除可能 | | |
| 不明形式 | review-only・削除拒否 | | |

※ **キー内で slot ID が引用符付きになる `genderMap` 系**を固定すること（GPT名指し）。

### API 形

```js
fix562.classifyKey(key, value)
// → { key, family:'live-story'|'story-backup'|'live-side-store'|…,
//     slotId, protection:'hard'|'protected'|'releasable', owner }

fix562.deletePolicy({ key, value, intent, path, authorization })
// → { allow:false, code:'live-data-requires-lifecycle-authorization',
//     classification:{…}, policyVersion:1 }
// ★物理削除は行わず、判断だけを返す
```

### 認可トークン（生セーブを消せる唯一の経路）

```js
authorization: { planId:'delete-slot-…', intent:'lifecycle-delete', owner:'fix402',
                 slotId:'smrg85jwsn6', allowedKeysHash:'…', createdAt:1785…, policyVersion:1 }
```

ゲート側で確認する7条件:
1. `authorization.slotId` と分類結果の slotId が一致
2. exact key が計画の許可集合に含まれる
3. 計画作成後に値の hash が変わっていない
4. `intent` が `lifecycle-delete`
5. 認可元が allowlist 内
6. 計画が期限切れでない
7. 同じ計画で未認可キーを削除しようとしていない

※ 生セーブ削除では、本体だけでなく**同じスロットのサイドストアを論理単位として扱う**。

### 最終的な安全指標（A3完了条件）

```
unapprovedDirectDeleteRequests = 0
unattributedPhysicalDeletes   = 0
gatewayで削除された生データ      = 0
gatewayで削除した全件に operationId あり
```

★ `bypassedOuter = 0` **だけを安全の証拠にしてはいけない**。
  A3が進むほど outer/inner の件数が減るのは正常（ゲート削除は native removeItem で影監視に映らないため）。
  総削除数 = legacy + gateway + clear の**実削除数**を別計上して合算する。

---

## 5. その他の残タスク

- **B**: 第2段（画像の変更検知をキー名→内容hashへ・原因A/C）／第3段（`localAvKeys()`/`localAv()` → IDB・原因B）／`chrAiAv4:` を同期対象に追加（原因D'）
- **C**: `cast.hero.name` が空になる根本原因は**未特定**（再現せず、あえて「直して」いない。fix573が検知・控え・警告のみ行う）／fix572後に `__gen_` 世代が再生成されるか確認／診断ログ(105KB)・`__v346raw`・`ab552p1A` の掃除
- **D**: 1MBソフト上限の自動GC／第4段 ホーム画面の管理画面化（**「作成上限」の話はここ。ハード上限ではなく可視化として**）／第5段 セット単位の復元
- **E**: surroundingChars診断／休眠ガード40件の仕分け／`collectIdentityEvidence`／別名の推移解決／rosterのターン番号異常／490文字ケース／同期の冪等キー

---

## 6. 環境まわりの地雷（毎回踏むので必ず読む）

- `index.html` に **NULバイトが1個**ある。編集は必ず Python の bytes で（テキスト処理で壊れる）。
- **git push は通らない**（`GH_TOKEN` は14文字の無効値）。出荷は **GitHub Web の Upload files** から。
  出荷後はコンテナ側で `git fetch && git reset --hard FETCH_HEAD`（そうしないと常に未コミット扱いに見える）。
- Pages の反映確認は `version.txt?cb=` を数回ポーリング（今回は2回目で反映）。
- **`?story=<id>` を付けずに index.html を開くと bootPull が走り、削除済み物語が D1 から復活する**。検証は必ず
  `index.html?v=<build>&story=smrg85jwsn6` で開く。
- モックの localStorage は `Object.keys()` でキーが見えるようにすること（`Object.defineProperty` で各キーを露出）。
  忘れると fix562 のような走査コードが「何も無い」と判断して素通りする。**2回踏んだ**。
- ChatGPT の長文は `window.__ans.slice()` だと内容依存でブロックされる。**スクリーンショットで読む**。
- fix569 の `stackOf()` は**自分のフレームを必ず除く**。除かないと全削除が canary 由来に見えて7経路が1件も数えられない。
