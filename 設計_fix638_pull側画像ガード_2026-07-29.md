# 設計 fix638 — pull側の画像ガード（blobで既存アイコンを上書きしない）

2026-07-29 / 対象ブランチ v292-rebuild / 実装番号 fix638（＋fix632 の格下げ）
確度ラベル: `確定` = 実コードで行位置まで確認済 / `高確度` = 強い根拠あり / `仮説` = 未検証

---

## 0. 結論（先に）

- 画像の正本は **per-key チャネル（D1 images + imageRev / fix523・fix633）** に一本化する。`確定`（GPT裁定）
- save blob（`op:put` の `pkg.idb`）は **正本ではなく最後の受け皿（recovery source）** へ格下げする。
- `fix399.applySave` の IDB 書き戻しに **pull側ガード（fix638）** を挟み、
  **ローカルに実体があるキーは blob では絶対に上書きしない**。
- fix632 は **`v292Dfix399_imgHash` へ二度と書かない**（診断専用へ格下げ）。初回シードの 5MB full 送信も廃止。
- fix564 スナップショット復元と fix399 selfHeal は **一切変更しない**（モード分離。そもそもガードを通らない）。

---

## 1. 症状と真因（保存層の機序）

### 1.1 現象
「同期すると、アイコンが以前のものへ戻る」

### 1.2 真因 `確定`（2026-07-29 に現コードで行位置を確認）

```
v292Dfix399-cloudsync.js:993（変更前 / 現 1009行目）
    return idbWriteAll(pkg.idb || {}).then(function(writeResult){
        ↓
v292Dfix399-cloudsync.js:217 idbWriteAll → :202 idbPutAllOneTx
    for (var i=0;i<keys.length;i++){ st.put(map[keys[i]], keys[i]); }
```

`idbPutAllOneTx` は **存在確認を1回もしない**。渡されたキーを全部 `put` する。
つまり applySave は **クラウド blob の画像で、ローカルIDBの既存キーを無条件に上書き**していた。

そして blob 側の画像が古いまま固定される理由が `v292Dfix399-cloudsync.js:619-620` `確定`：

```js
var curHash = hash(imgKeys.slice().sort().join('|'));
var needFull = force || (curHash !== imgHashStored()) || (workerVer < 11);
```

full 送信（＝`pkg.idb` に画像本体を載せる）の判断が **キー名の集合ハッシュだけ**で、
中身を1バイトも見ていない。同じキャラの絵を作り直してもキー名は変わらないので blob は更新されない。

**古い blob が pull で降ってきて、手元の新しい絵を潰す。** これが症状の保存層側の機序。

### 1.3 補足（自動push経路の実態）`確定`
fix402 が `v292Dfix399AutoOff='1'` を書くため、fix399 の `push()` は **手動☁️のときしか走らない**。
よって fix632 の `dirty632:` マーカーは自動では消費されない（＝ full 送信は起きない）一方、
`applySave:1016` が `v292Dfix399_imgHash` を実在庫hashで上書きするので、
**取り込みが起きただけでマーカーが消える＝送っていないのに成功扱い**になっていた。

---

## 2. 判定規則（GPT裁定そのまま）

`normal-sync`（＝`fix399.applySave` 経由の通常pull）のときだけ適用する。優先順は裁定どおり。

| # | 条件 | 判定 | 理由コード |
|---|---|---|---|
| ① | ローカルに実体がある | **書かない** | `local-exists` |
| ② | 削除墓標がある | 書かない | `tombstone` |
| ③ | per-key remote(manifest) に該当キーがある | 書かない（rev/hash規則に委ねる） | `per-key-remote` |
| ④ | いま必要とされていない | 書かない | `not-needed` |
| ⑤ | 不在＋必要＋墓標なし | **補充として書く**＋backfill記録 | `recovery-candidate` |

- ①を最優先に置いた。ローカル実体がある限り、他の条件が何であっても書かない（最強の不変条件）。
- per-key manifest が取れない（未ログイン・通信失敗・fix633未ロード）ときは `remoteKnown:false` として記録し、
  **不在キーの補充だけは許す**。不在キーへの書込みは上書きではないので、この症状を再発させない。`高確度`

### 2.1 「ローカルに実体があるか」の判定 `確定`
false-empty（読めなかっただけなのに「無い」と判断する）で上書きしてしまわないよう、**3経路の union** を取る。
1. `fix399.idbReadKeys()`（IDBの実キー。**失敗時も `[]` を返す**ので単独では信用できない）
2. `__v292av.keys()`（fix631 の在庫キャッシュ。IDB由来 ∪ 生localStorage）
3. `localStorage.getItem('v292av2_<pk>')`（fix346 のラッパ経由 mem キャッシュ。同期で確実）

どれか1つでも「ある」と言えば **ある** 扱い。

### 2.2 「いま必要か」の判定 `確定`
- `v292Dfix399_imgKeys`（期待集合。fix631 が実在庫で単調にシードしている）
- 画面に映っている `img[data-avpk]`
- **`v292avrec_<pk>`（レシピ）があるキャラ**

3つ目が要。`applySave` は **localStorage への書き戻し（:1007）を済ませてから** IDB を書く（:1009）ので、
ガードが走る時点で **クラウド由来のレシピは既にローカルにある**。
これが無いと「一度も取り込んでいない新品の端末」で期待集合が空になり、**blobからの復元が丸ごと止まる**。
（回帰テスト `★★新品端末の復元が壊れていない` で固定）

### 2.3 削除墓標について `確定`
2026-07-29 時点、**アイコン単位の削除経路はこの製品に存在しない**（`removeItem('v292av2_...)` の呼び出しは
fix346 のラッパ定義のみ。削除を発行する箇所は無い）。よってこの集合は通常いつも空。
将来 削除を作ったときに blob から黙って蘇らないよう、受け口だけ先に用意した：
`v292Dfix638_tomb`（配列）と `window.__chronicleImgTombstones.keys()`。

---

## 3. モード分離（GPT指定2）

| モード | 経路 | ガード | 根拠 |
|---|---|---|---|
| `normal-sync` | `fix399.applySave` ←（fix402 自動pull / fix399 bootPull / 手動⬇️） | **適用する** | 症状の発生経路 |
| `explicit-restore` | `fix564.restore({confirm:true})` | 素通し | manifest exact hash 照合＋ユーザー意思＋退避＋read-back照合がある |
| `self-heal` | `fix399.selfHeal` → `idbPutAllOneTx` 直呼び | 素通し | 不足キーだけを補充する経路。既存を触らない |

**`idbWriteMissingOnly` による一律置換はしていない。** ガードは `applySave` の1点にだけ挿し、
モードが `normal-sync` 以外なら `guardedWrite` は全キーをそのまま下へ渡す。
fix564 と selfHeal は **そもそもこの関数を通らない**（構造的に無関係）ので、二重に安全。

---

## 4. ★全IDB書込み経路の監査表（GPT指定4）

対象ストア: `IndexedDB chr6av / imgs`（キーは `v292av2_<pk>` の**フルキー**）

| # | 経路 | 実装位置 | 無条件上書きが残るか | 対処・理由 |
|---|---|---|---|---|
| 1 | **fix399 applySave の blob 適用** | `v292Dfix399:1009 → idbWriteGuarded → idbWriteAll → idbPutAllOneTx` | **残らない（本fixで封じた）** | fix638 のガードを通す。既存キーは `local-exists` で skip |
| 2 | fix399 selfHeal | `v292Dfix399:1194 idbPutAllOneTx(map, mk)` | 形式上は残る | **不足キー（`expected` にあって IDB に無い）だけ**を map に入れるので、既存を上書きしない。加えて既定 OFF（`v292Dfix399SelfHealOn='1'` で opt-in）。GPT裁定「selfHeal を壊さない」に従い変更しない |
| 3 | fix346 の直書き（正規の書込みファネル） | `v292Dfix346:idbPut` ← `localStorage.setItem('v292av2_*')` | 残る（**意図どおり**） | ここは「この端末で決めた絵」を書く唯一の正規経路。fix197生成 / fix523.pullOne / fix516 / fix517 / fix474採用 が全部ここへ集まる。上書きされるのは**自分で決めた値**なので問題ない |
| 4 | fix523 pullOne（per-key 受信） | `v292Dfix523:81 localStorage.setItem(PREFIX+pk, durl)` → 経路3 | 残る（**意図どおり＝正本**） | `server.rev > 既知rev` のときだけ上書きする。巻き戻りは rev で原理的に排除済み。fix638 は**直列化だけ**を足す |
| 5 | fix633 sweep | 自分では書かない。`f523.pullOne / pushOne / revSet` に委譲 | 経路4に同じ | 既定 shadow（`v292Dfix633Live='1'` の端末だけ実弾）。伝播の正しい経路 |
| 6 | fix564 スナップショット復元 | `v292Dfix564:206 localStorage.setItem(plan[i].to, val)` | **画像は対象外** | `partKeys()` は「スロットIDを含むキー」だけを集める。`v292av2_*` はスロットIDを含まないので**スナップショットに入らない**。＝画像を復元も破壊もしない |
| 7 | home.html の pull | `home.html:707 call({op:'get'})` → `r.data.ls` のみ書く | **画像を書かない** | `data.idb` を一切参照していない（実測: home.html に `idb` の文字列なし）。ホームは本文だけ |
| 8 | import（セーブ取り込み） | 専用の画像 import は**存在しない** | — | 画像を含む取り込みは経路1（cloudsync applySave）のみ。棚卸しで確認 |
| 9 | fix346 migration | `v292Dfix346:migrate()` | 残る（**意図どおり**） | 生localStorage に残っていた `v292av2_*` を IDB へ移す1回限りの移送。`if(!(k in mem))` で既に mem/IDB にあるものは上書きしない |
| 10 | fix515 icon-persist-probe | `IDBObjectStore.prototype.put` をラップ | 書かない（観測のみ） | `_put.apply` で素通し。記録するだけ |
| 11 | fix474 candidate | 別DB `chr6cand`（records/baseline/journal） | 対象外 | `chr6av/imgs` を触らない。採用時は経路3を通る |

**残る無条件上書きは 2/3/4/9 の4経路。いずれも「自端末が決めた値」または「rev で新しいと確認できた値」を書く経路**であり、
今回の症状（**古い blob が新しいローカルを潰す**）を起こす経路ではない。
唯一それを起こしていた経路1だけを封じた。`高確度`

---

## 5. 画像適用の直列化（GPT指定5）

per-key適用（fix523/633）と blob適用（applySave）が同時に走ると後勝ちになる。
**完全な排他は作らない。後勝ちの窓を狭めるだけ。**

- 共通 single-flight ロック `window.__chronicleImgApplyLock` を1本だけ置く（実体は fix638 内）。
- blob適用は **IDB書込みの間だけ** ロックを取る（`serializeP('blob-apply', …)`）。
- fix638 が `__v292Dfix523.pullOne / pushOne` を包み、ロック中なら **150ms×最大10回**待ってから走る。
  待ちきれなければ **そのまま走らせる**（止めるのが目的ではない）。
- ロックは 8秒で自動失効する。片方が落ちても画像更新が永久に止まらないため。

**限界（明記）**: fix523 自身の内部 `recvSweep` は**内部関数**の `pullOne` を呼ぶので包めない。
fix633 は毎回 `W.__v292Dfix523` を引き直して呼ぶので包める。
残る窓は fix523 の 20秒周期スイープのみで、しかも blob 側は既存キーを書かなくなったため衝突面積は極小。`高確度`

OFF: `v292Dfix638LockOff='1'`（直列化ラップだけを止める）

---

## 6. fix632 の格下げ（GPT指定3）

### 6.1 何を止めたか
| 項目 | 変更前 | 変更後 |
|---|---|---|
| `v292Dfix399_imgHash` への書込み | `'dirty632:<ts>'` を書いて full 送信を誘発 | **一切書かない** |
| 検知結果の置き場 | 同上（同期の判断を汚す） | 自前キー `v292Dfix632_diag`（診断専用・直近8件＋総数） |
| 初回シード | 一度だけ dirty 化＝約5MB の full 送信 | **廃止**（正本は per-key なので不要） |
| `isDirty()` | マーカーの有無 | **常に false**（同期を汚す dirty は存在しない） |

**なぜ格下げか**: `applySave:1016` が imgHash を実在庫hashで上書きするため、
「取り込みが起きただけでマーカーが消える＝送っていないのに成功扱い」が構造的に避けられない。
書かなければこの穴は原理的に塞がる。伝播の正しい経路は fix633 の Live 化。

### 6.2 既に `dirty632:` が書かれた端末の後始末
起動7秒後に1回だけ `clearLegacyMarker()` を走らせる。

- `v292Dfix399_imgHash` が `'dirty632:'` で始まるときだけ動く。
- **IndexedDB `chr6av/imgs` のキー集合**を読み、`fix399:62` と**同じ式**で hash を作り直して書き戻す。
  `hash(keys.slice().sort().join('|'))`, `h=((h<<5)-h+c)|0 → String(h>>>0)`
- **IDBを読めなければ触らない**（適当な値を書くと、それはそれで full 送信を誘発するため）。
- 残置しても実害は「手動☁️を押したとき1回だけ 5MB の full 送信」に限られる（自動pushは fix402 で imgHash を見ない）。

OFF: `v292Dfix632Off='1'` → ラップも診断も後始末も全部止まる。

---

## 7. fix399 への変更（最小・行単位）

`v292Dfix399-cloudsync.js` への差分は **2箇所だけ**。

**(A) 追加**: 236〜255行目（`idbWriteAll` の直後・`// ---- サーバー通信 ----` の直前）に 15行（うち註釈5行 / `function idbWriteGuarded` は 241行目）

```js
function idbWriteGuarded(map, ctx){
  try {
    var g = window.__v292Dfix638;
    if (g && g.__armed && typeof g.on === 'function' && g.on() && typeof g.guardedWrite === 'function'){
      return Promise.resolve(g.guardedWrite(map, ctx || { mode: 'normal-sync', path: 'fix399.applySave' },
                                            { writeAll: idbWriteAll, readKeys: idbReadKeys }));
    }
  } catch(e){}
  return idbWriteAll(map);          // ★未ロード / OFF → 従来どおり
}
```

**(B) 置換**: 旧 993行目 → 新 1009行目、1行（直前に註釈1行を追加）

```diff
- return idbWriteAll(pkg.idb || {}).then(function(writeResult){
+ return idbWriteGuarded(pkg.idb || {}, { mode: 'normal-sync', path: 'fix399.applySave' }).then(function(writeResult){
```

他は1バイトも変えていない。`push()` / `collectFull()` / `selfHeal()` / `backupBeforeApply()` は無変更。

### 7.1 期待集合を縮めない（実装で踏んだ落とし穴）`確定`
`idbWriteAll` は内部で `saveExpectedImgKeys(渡されたキー)` を呼び、
`v292Dfix399_imgKeys` を **渡した部分集合で置換**する。
ガードでキーを減らすとそのまま期待集合が縮み、fix631 が守っている単調性
（縮める＝削除処理の新設＝破壊的変更）を壊す。

→ fix638 は **`writeAll` の後に** `union(既存, blobの全キー)` を書き戻す。
順序を逆にすると即座に上書きされて縮む（回帰テストで実際に検出した）。

---

## 8. 反証・リスク・未確認

| # | 内容 | 確度 | 影響 | 手当て |
|---|---|---|---|---|
| R1 | per-key manifest を取れない端末では `remoteKnown:false` になり、不在キーを blob から補充する。その絵が古い可能性がある | `高確度` | 小 | 上書きではないので症状は再発しない。書いた分は backfill で per-key へ昇格し、以後 rev 規則が働く |
| R2 | `verify()`（`__v292Dfix399x.verify`）は「blobの全キー」を期待値にするので、ガードが skip した分を `missing` と報告する | `確定` | 診断のみ | 挙動に依存する箇所は無い。実機では `__v292Dfix638.lastDecision()` を併せて見る |
| R3 | fix523 内部 recvSweep との後勝ちの窓が残る | `確定` | 小 | §5 に明記。blob 側が既存キーを書かなくなったので衝突面積は極小 |
| R4 | アイコン削除機能を将来作ったとき、`v292Dfix638_tomb` へ登録し忘れると blob から復活し得る | `仮説` | 中 | 受け口を先に用意した。削除機能を作る際の必須手順として記録する |
| R5 | 本fixは**再生成した絵が他端末へ伝わる**問題そのものは解決しない（それは fix633 の Live 化） | `確定` | — | 本fixは「戻らないようにする」まで。伝播は per-key チャネルの担当 |

---

## 9. 検証（実機コンソール）

```js
// ① ガードが載っているか
__v292Dfix638.status()
//   {armed:true, on:true, wrapped523:true, backlog:0, ...}

// ② 直近の取り込みで何を書いて何を弾いたか（★これが本命）
__v292Dfix638.lastDecision()
//   counts に local-exists / per-key-remote / not-needed / recovery-candidate の内訳が出る
//   ★ local-exists が1件以上あれば「古いblobで潰されるはずだったのを守った」証拠

// ③ いま pull が来たら何が起きるかの下見（副作用なし）
__v292Dfix638.plan(['v292av2_<pk>', ...]).then(r => console.log(r.why))

// ④ 補充したキーの正本昇格待ち
__v292Dfix638.backlog()
__v292Dfix638.flushBackfill(r => console.log(r))

// ⑤ fix632 が同期を汚していないこと
__v292Dfix632.status()
//   dirty:false / legacyMarker:false / legacyCleared:{acted:…} / noted:<検知件数>
localStorage.getItem('v292Dfix399_imgHash')   // ★10進数字列であること（dirty632: で始まらない）

// ⑥ 在庫と期待集合（縮んでいないこと）
__v292Dfix631.status()      // inventory と expected の件数
JSON.parse(localStorage.getItem('v292Dfix399_imgKeys')).length

// ⑦ 緊急停止（fix637時点の挙動へ完全復帰）
localStorage.setItem('v292Dfix638Off','1'); location.reload();
```

---

## 10. 回帰テストで固定した契約（test_fix638.cjs / test_fix632.cjs）

- C1 ★ローカルに実体があるキーは blob で**絶対に上書きされない**（**本物の applySave を通して**確認）
- C2 ★per-key remote にあるキーは blob から書かない
- C3 ★不在＋必要＋墓標なし のときだけ補充し、backfill 台帳に載せる
- C4 ★墓標のあるキーは復活しない
- C5 ★必要とされていないキーは復活しない
- C6 ★explicit-restore / self-heal は素通し
- C7 ★OFF で fix637 時点と完全に同じ挙動（無条件上書きへ戻る）／未ロードでも壊れない
- C8 ★期待集合 `v292Dfix399_imgKeys` を縮めない
- C9 ★判定できないときは書かない（fail-closed）。applySave は落ちない
- C10 ★ロック中は per-key 適用がすぐには走らず、空けば走る
- C11 ★補充キーは `fix523.pushOne` へ PREFIX 無しの pk で渡され、台帳から落ちる
- C12 ★fix564 の復元は従来どおり既存を置換できる
- （fix632）★`v292Dfix399_imgHash` へ二度と書かない／初回シードの full 送信をしない／
  旧 `dirty632:` は実在庫hashへ戻し、IDBを読めないときは触らない

---

## 11. 意図的にやらなかったこと

- `idbWriteAll` を `idbWriteMissingOnly` へ一律置換すること（GPT裁定で明示的に禁止）
- `fix399.selfHeal` / `fix564` / `home.html pull` の変更（壊さないと決めた領域）
- 完全な排他ロック（後勝ちの窓を狭めれば足りる、という裁定に従った）
- BUILT / version.txt の更新、デプロイ、push（出荷はおしんの手番）
- アイコン単位の削除機能の新設（受け口だけ用意し、削除処理そのものは作らない）
