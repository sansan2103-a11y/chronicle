# Cloudflare Worker の更新手順（1〜2分・貼るだけ）

## これは何？

物語を削除したとき、**古いキャッシュを持った端末が「削除した」という記録をうっかり消してしまう**のを
サーバ側で防ぐ変更です。クライアント側（Chronicle本体）の対策はすべて完了していて、これは最後の保険です。

## ★急ぎではありません

墓標（削除の印）の仕組みはまだ同期につないでいないので、**今すぐ入れなくても何も壊れません**。
つないだあとに入っていれば十分です。手が空いたときで大丈夫です。

## 手順

1. Cloudflare のダッシュボードを開く → **Workers & Pages** → **novel-proxy**
2. 右上の **Edit code**（またはクイック編集）
3. エディタ内で全選択（Ctrl+A / ⌘+A）して、渡したファイル
   **`chronicle-proxy-v24_tombstone.js`** の中身を**まるごと貼り付け**
4. **Save and Deploy**

以上です。設定値の変更や、環境変数の追加はありません。

## 何が変わるのか（1箇所だけ）

「版番号を名乗らない書き込み要求」が来たとき、**保存済みのデータに削除の印が付いていたら、
上書きせず別枠に取っておく**という判定を1つ足しただけです。

- 拒否ではなく**別枠保存**です。このWorkerの「データは絶対に消さない」方針をそのまま守ります。
- 判定に失敗したときは**何もしません**（通常の保存は今までどおり通ります）。
  判定できないことを理由に保存を止める方が害が大きいためです。
- 既存の判定・機能は1つも変えていません。

## 元に戻したいとき

同じ手順で `chronicle-proxy-v23b-imgsync.js` を貼り直せば元どおりです。

## 動いているか確かめたいとき

確かめなくて大丈夫です。もし気になったら、Chronicle を開いて開発者コンソールで
`__v292Dfix580.report()` を実行すると、同期の状況が日本語で出ます。
