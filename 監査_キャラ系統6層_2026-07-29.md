# 監査：キャラクター系統 4症状 × 6層（2026-07-29）

対象：`v292-rebuild` / HEAD = `923937d`（fix630）/ 実配信の script タグ 225本を母集団とする静的監査。
方法：`index.html` の `<script src>` を全列挙して**実際に読み込まれるファイルだけ**を対象にし、
`S.cast` への書込・読出経路を全数追跡した。旧 `v1xx`/`v2xx-*.js` は index.html から外れているので対象外。

**このファイルは「どこが壊れているか」の台帳である。直したものは §7、直さなかったものは §8。**

---

## 0. 先に結論（3行）

1. **`window.S` は永久に `undefined`**（`index.html` の `S` はトップレベル `const`）。にもかかわらず
   `window.S` を**唯一の経路**にしている生きたモジュールが5つ残っており、そのうちの1つが
   **`S.cast` を人の操作なしに埋める唯一の経路**だった。＝症状1の機序。`確定`
2. 「登場しているか」の判定が**モデルへ渡る側**と**画面に出る側**で**証拠の集合が違う**。
   画面側（fix145/fix520）は会話ログの確定話者を見るが、モデル側（fix95）は見ない。＝症状3の主因候補。`高確度`
3. 「一度きりの存在は保留」という規則が **fix307 ではプロンプトの文言にしか無く、コード側に保留が無い**。
   ＝症状4（呼称違いの増殖）が止まらない構造。`確定`（コードに保留分岐が存在しないこと自体は確定）

---

## 1. 症状 × 6層 監査表

層の定義：①モデルへ渡す情報の不足・矛盾 ②出力解析・登録時 ③保存書き込み時 ④復元時 ⑤表示のみ ⑥fix同士の競合

| # | 症状 | 層 | 所在 | 事実 | 確度 |
|---|---|---|---|---|---|
| A | 1 | ③ | `features.js:1209`（§8 auto_bootstrap `maybeBootstrap`） | `if (!window.S \|\| !S.cast) return;` — `window.S` は永久に undefined。**モジュール全体が一度も走っていない**。`S.cast` を人の操作なしに埋める唯一の経路がこれ。 | `確定` |
| B | 1 | ③ | `features.js:1133 / 1181` | 同 §8 の `extractFromState` / `applyBootstrap` も同じ hard gate。API は `window.__v292.autoBootstrap` に公開されているが、呼んでも `null` しか返らない。 | `確定` |
| C | 1 | ③ | `S.cast.npcs` への生きた書込経路の全数 | ①設定フォーム（`features.js:937` / fix351:71）②おまかせ（fix335:185）③キャラ一覧の「NPC登録」（fix145:449）④キャラ生成wizard（`features.js:8308`）⑤§8 auto_bootstrap（`features.js:1194`）。**⑤以外はすべて人の操作が要る。**「3ターン回して `npcs:[]`」は⑤が死んでいれば必然。 | `確定` |
| D | 1 | ③ | `cast.hero.name` が空になる件 | fix573 が「あった→無くなった」瞬間の検知と控えを既に持っている（`v292Dfix573_log`）。**が、この監査では*空になる書込*そのものは静的に特定できなかった。**「新しい物語では最初から空」なのか「途中で消えた」のかは fix573 のログでしか区別できない。 | `要検証` |
| E | 1 | ⑥ | fix606 / 611 / 616 / 620 / 624 | 登録経路への副作用を疑って全数確認した。**606/611/624 は読み取り専用**、616 は `turn._convSayMeta` を足すだけ、620 は `_convSays[].who` だけを書く。**5本とも `S.cast` に一切触らない。**時期の一致は偶然。 | `確定` |
| F | 2 | ③④ | `fix135:174` / `fix307:139` | 「S がこのスロットの物語か」の検証が `scene.loc` の一致**だけ**で、`if (blobLoc && curLoc && blobLoc !== curLoc) return;` の形。**新しい物語は `scene.loc` が空なので、この守りが丸ごと素通しになる。**要約後の再確認 `(s2.scene.loc\|\|null) !== startLoc` も `null !== null` で通る。＝別物語の longmem / ロスターが新しい物語へ入る窓が、**新しい物語でだけ開いている**。 | `高確度` |
| G | 2 | ⑤ | `fix145:303`（`__isAliasOfRegistered`） | 登録名と worldinfo 名の**双方向の部分一致**（`nm.indexOf(_r)>=0 \|\| _r.indexOf(nm)>=0`、2字以上）で一覧から隠す。fix533 が存在判定でこの素の `indexOf` を危険と断じて直した**同じ型**が、別名判定側に残っている。「アン」登録で「アンナ」が消える等。 | `高確度` |
| H | 2 | ⑤ | `fix145:310`（fix358 `__isVariantOfPeer`） | 末尾一致で**長い方を隠す**。「鏡の男」と「男」が並ぶと**具体的な方が消えて曖昧な方が残る**。統合の向きが逆。 | `高確度` |
| I | 2 | ⑤ | `fix298:57`（`quasiRoster`）vs `fix277:535`（`quasiRecent`） | **同じ台帳の閾値が2つある。**画面は `seen.length>=1` で出し、sys 注入は `seen.length>=3`。＝「一覧には居るのにモデルは知らない」が**設計上必ず起きる**。 | `確定` |
| J | 3 | ① | `features.js:9530`（fix95） | 登録NPCは「名前が **playerText + 直近8ターンの narrative** に出るまで休眠（sticky）」。**`_convSays` を見ない。**画面側 `findLastTurnForName` は fix520 で `_convSays` を証拠に加え済み。→ 姓＋名で登録し本文が下の名前だけの人物は、会話ログで喋っていても**モデルへ渡らない**（fix573 の「白石澪／澪」がまさにこの形）。 | `高確度` |
| K | 3 | ① | 同上 | 設定で登録しただけでまだ本文に名前が出ていないNPCは、初手から全員休眠。`active.length===0` なら**モデルはNPCを1人も知らない状態で1ターン目を書く**。これは fix95 の設計意図（AI Dungeon の Story Card 方式）でもあるので、**バグか仕様かはおしんの裁定事項**。 | `要検証` |
| L | 3 | ① | `features.js:1405` / `9027` | §9 memory(v219) の `buildStateBlock()` と fix50 の `getRoster()` も `window.S` hard gate。前者は fix77 が実質置き換えているが、**fix50 の声紋ブロックは丸ごと空**（fix377 が別経路で声を扱ってはいる）。 | `確定` |
| M | 4 | ② | `fix307:121`（`mergeRoster`） | **「一度しか出ておらず再登場が読み取れない存在は載せない(保留)」は SYS 文字列の中だけの規則**で、`mergeRoster` には保留分岐が無い。LLM が返した呼称は `handle` の完全一致以外**必ず新規行として即追加**され、`installWiShim` で一覧へ出る。「鏡から出る腕／鏡の男／大きな影」が3行になるのはここ。 | `確定` |
| N | 4 | ② | `fix277:396`（fix537 `DESCRIPTIVE_TAIL`） | 名乗りによる同一性確定の対象は `少女\|少年\|女\|男\|子供\|子ども\|娘\|息子\|影\|人影\|老人\|老婆\|青年\|婦人` で**終わる**呼称だけ。「鏡から出る**腕**」「濡れた**手**」「無数の**目**」等の**身体部位型の呼称が対象外**。 | `確定` |
| O | 4 | ⑥ | fix307 と fix277 の関係 | fix307（LLM生成の呼称）と fix277（`<say who>` 実測の呼称）は**互いの台帳を見ない**。同一存在が両方に別表記で入り得るが、名寄せは fix145 の表示側でしか行われない。 | `高確度` |
| P | 全 | ⑥ | `fix459:36`（MARKERS） | sys ブロック境界の既知マーカーは43件。実配信 js が出す `【…】` 見出しはそれより**明らかに多い**（例：`【キャラ点呼＝消失禁止】`『features.js』、`【場にいる者は"生きている"】`『features.js/fix440』、`【話者タグの徹底（最優先）】`『fix514』、`【準登録キャラ(自動)】`『fix277』、`【身体状態・正史(絶対に覆らない)】`『fix333』）。未知マーカーは**直前の既知ブロックへ吸収される**ので、直前が dropA〜D の対象なら**道連れで消える**（fix496 の真因そのもの）。実際に消えているかは**送信時の並び順に依存する**ので、静的には決められない。 | `要検証` |
| Q | 1 | ③ | `fix600:61 / 91` | 2026-07-27 の「新しい物語に別の物語の20ターンが入る」を止めるガードが `var S = window.S` で、**`memTurns()` は常に -1・`wrapSave()` は常に false**。120回ポーリングして「S.save を包めませんでした」で終わる＝**一度も武装していない**。 | `確定` |
| R | 1 | ① | `features.js:954`（seed_aware `hasApiKey`） | `if (!window.S \|\| !S.cfg) return false;` → 常に false → `runEnhance` が毎回「no API key — keep base random fill」で抜ける。おまかせの LLM 種膨らませ（`features.js` §7）は**動いていない**。ただし fix117（`features.js:10403`）と fix436 が別経路で AI 生成を担っており、こちらは正しく `S` を取れている。**復活は API 呼び出し＝費用が動くので、おしんの裁定事項。** | `確定`（死んでいること）／`要検証`（実害の有無） |

### `window.S` を唯一の経路にしている生きたモジュール（全数）

| 所在 | 影響 | 本監査での扱い |
|---|---|---|
| `features.js` §8 auto_bootstrap（1133 / 1181 / 1209） | cast の自動アンカーが丸ごと死 | fix637 で**観測**（既定では書かない） |
| `features.js` §9 memory v219（1382 / 1405） | 「🧠 キャラクター継続状態」sys ブロックが常に空 | 復活させない（fix77 が実質置換。復活すると sys が二重化する） |
| `features.js` §10 state_inference（1588〜1798） | 既定 OFF なので実害小 | 触らない |
| `features.js` §7 seed_aware（925 `syncStateFromForm` / 954 `hasApiKey`） | おまかせの LLM 膨らませが不発 | 触らない（費用が動く → 裁定待ち） |
| `features.js` fix50（9027 `getRoster`） | 声紋ブロックが常に空 | 触らない（fix377 が別経路で担当） |
| `fix600`（61 / 91） | 新しい物語の保存ガードが不発 | **fix635 で直した** |
| `fix247`（99 `ensureSentinel`） | プロキシ sentinel 充填が不発 | **既に fix336 が肩代わり済み**（対処不要） |

> これは新種のバグではない。`fix333i` と `fix336` が 2026-07-01 に同じ型を踏んでいる（fix336 の冒頭コメントに記録あり）。
> **掃き残しが5箇所あった**、というのが今回の発見。

---

## 2. 症状1の真因（最優先）

### 事実の連鎖

```
index.html:  const S = { ... }         ← トップレベル const。window.S には出ない
        ↓
features.js §8 auto_bootstrap:
        maybeBootstrap(){ if (!window.S || !S.cast) return; ... }   ← ここで必ず抜ける
        ↓
「設定を通さずに始めた物語」で cast.hero.name / cast.npcs / scene.loc を埋める者が居なくなる
        ↓
・cast.hero.name = ''   ・cast.npcs = []   のまま何ターンでも進む
        ↓
・castLock / fix459 blockA / fix307 の「主役は載せない」判定が全部「登録キャスト＝0人」で動く
・fix573 が記録した「本文の澪も白石澪も登録キャストに一致せず自動抽出へ落ちて2件に分裂」と同じ形になる
```

### `cast.hero.name` が空になる件について（宿題の現状）

- 「**新しい物語では最初から空**」は `const S` の初期値そのものなので **確定**。
- 「**途中で空に上書きされた**」かどうかは、この監査では**特定できなかった**。
  `fix351.commitDraft` は `if (hn != null) S.cast.hero.name = hn.trim();` なので、
  **設定モーダルの `#cfgHName` が空のまま閉じる操作が入れば空で確定する**経路は存在する（`確度: 高確度`）。
  fix573 はまさにこれを捕まえるために作られている。**実機のログを見るまで断定しない。**

---

## 3. 症状2の真因候補

1. **F**（`scene.loc` を物語同一性の代理にしている）— 新しい物語で守りが素通し。`高確度`
   → 「別物語の情報が残る」が**新しい物語でだけ起きやすい**理由を説明できる。
2. **I**（一覧 `seen>=1` / sys `seen>=3` の閾値二重）— 「一覧と実際の登場状況の不一致」の直接原因。`確定`
3. **G / H**（表示側の別名判定が素の部分一致・末尾一致）— 「登場キャラが一覧に入らない」。`高確度`
4. fix529 の既知の取りこぼし（区切りの無い名詞句を別名証拠にしない）はコメントに明記済み。**おしんの判断待ちのまま。**

## 4. 症状3の真因候補

1. **J**（fix95 が `_convSays` を見ない）— **fix636 で直した**。`高確度`
2. **K**（登録しただけのNPCが初手から全員休眠）— 仕様との境目。`要検証`
3. **I**（sys 側の準登録閾値 3ターン）— 「後から初登場扱い」の説明になる。`確定`
4. **P**（fix459 が `【キャラ点呼＝消失禁止】` `【場にいる者は"生きている"】` を道連れで落としている可能性）`要検証`

## 5. 症状4の「保留に入るべきなのに新規登録される分岐」

**特定できた分岐は1つ。`v292Dfix307-npc-roster.js:121 mergeRoster`。**

```js
if(byKey[h]){ ...更新... }
else { var o={handle:h, ...firstTurn:ct, lastTurn:ct}; byKey[h]=o; existing.push(o); }   // ★保留が無い
```

- SYS には「一度しか出ておらず再登場が読み取れない存在は載せない(保留)」と書いてあるが、
  **判定がモデル側にしか無い。**モデルが返せば無条件で台帳に載り、`installWiShim` で一覧に出る。
- 対して fix277 の準登録は `seen.length >= 3` という**コード側の保留**を持っている。
  つまり**「保留の仕組みは既にプロジェクトにあるのに、fix307 だけがそれを持っていない」**。

### 提案（★実装しない。おしんの裁定が要る）

強制統合を一切増やさずに、保留側へ寄せるだけの案：

- **案1（最小）**：`mergeRoster` の新規追加を `pending: true` で入れ、
  **次の抽出ラウンド（`INTERVAL`=3ターン後）でも同じ呼称が返ってきたときだけ** `pending` を外す。
  外れるまでは `installWiShim` の合成対象にしない。＝**SYS が言っている規則をコードで担保するだけ**。
  同一性の判断は一切しないので「類似する別個体を強制統合しない」に抵触しない。
- **案2（併用）**：新規追加時に fix529 と同じ存在確認（`findLastTurnForName` 相当）を通し、
  **その呼称が本文・入力・会話ログのどこにも文字列として現れないものは載せない**。
  LLM が要約から作った呼称（本文に無い言い換え）を弾ける。
- **案3（症状4-N向け）**：fix537 の `DESCRIPTIVE_TAIL` に身体部位（`腕|手|指|足|目|眼|顔|口|髪|声`）を足す。
  ただしこれは**統合の対象を広げる**変更なので、おしんの「類似する別個体まで強制統合しない」に触れる。
  **名乗りが成立したときだけ**という他の3条件は維持されるので安全側だが、**裁定を仰いでから**。

---

## 6. 実装した最小修正（§7）と、しなかったもの（§8）

## 7. 実装した修正

| fix | 何を直したか | 挙動の変化 | OFF |
|---|---|---|---|
| **fix635** `v292Dfix635-new-story-guard-rearm.js` | fix600 の保存ガードを、正式API（`__chronicleGetState`）で**武装し直す**。判定条件・文言は fix600 と1文字も変えていない。 | `?new=1` の物語で「ディスク0ターン なのに メモリ2ターン以上」のときだけ保存を止める。それ以外は完全に素通し。**消さない・上書きしない。** | `v292Dfix635Off='1'` / `v292Dfix600Off='1'` |
| **fix636** `v292Dfix636-npc-presence-evidence.js` | 登録NPCの `appeared`（fix95 の sticky フラグ）を、**会話ログの確定話者 `_convSays[].who` と完全一致したときにも**立てる。fix145(fix520) と証拠の集合を揃えるだけ。 | 向きは常に「居ることにする」のみ。**誰も除外しない。**部分一致・短縮名・別名では絶対に立てない。`_convSays` / `say` / `who` / DOM は触らない。 | `v292Dfix636Off='1'` |
| **fix637** `v292Dfix637-cast-bootstrap-probe.js` | 「3ターン以上進んで `cast.hero.name` が空」を**その場で警告**し、休眠している経路を実測で一覧する。§8 が書くはずだった内容を `proposal()` で見せる。 | **★既定では1バイトも書かない。**書くのは `v292Dfix637Apply='1'` を入れて `apply()` を呼んだときだけ。 | `v292Dfix637Off='1'` |

テスト：`test_fix635.cjs`(40) / `test_fix636.cjs`(41) / `test_fix637.cjs`(45)。
`node run_all_tests.cjs` → **58ファイル / 合格 3180 / 失敗 0 / サマリ不明 0 / 異常終了 0**。
（`test_fix547.cjs` の「`__chronicleGetState` を参照するのは N ファイル」台帳を 28→31 に更新。
　この台帳は「新しく S を読むモジュールは必ず正式API経由にする」という契約そのものなので、
　新規3本を足したら必ず更新する側。）

**デプロイはしていない。`git push` もしていない。`BUILT` と `version.txt` は触っていない**（デプロイ時の作業のため）。

## 8. 直さなかったもの と その理由

| 対象 | 直さなかった理由 |
|---|---|
| §8 auto_bootstrap の**復活そのもの** | 復活させると「本文から推測したカタカナ名」が**主人公名としてセーブデータに確定する**。おしんが意図した主人公と違う名前が入り得て、後から気づきにくい。`cast.hero.name` が空になる真因（D）が未特定のまま復活させるのは**対症療法**にあたる。→ fix637 で**観測と提案だけ**にし、書き込みは明示許可制にした。 |
| §9 memory(v219) の復活 | 「🧠 キャラクター継続状態」ブロックが復活すると、fix77 の「【各キャラの現在の状態】」と**同じことを言う sys ブロックが2つ**になる。fix459/fix500 が潰してきた重複クラスタを自分で増やすことになる。 |
| §7 seed_aware `hasApiKey` の復活 | 復活＝**API 呼び出しが1本増える＝費用が動く**。CLAUDE.md の「費用が動く判断は止まって確認」に該当。加えて fix117/fix436 が同じ役割を別経路で果たしているので、二重生成になる恐れがある。 |
| fix50 `getRoster` の復活 | 声紋ブロックが復活すると sys が伸び、fix377 の声アンカーと重複する。効果より副作用の方が読めない。 |
| fix459 MARKERS の追加（P） | **どのブロックが実際に落ちているかを静的に確定できなかった。**送信時の並び順に依存する。当てずっぽうでマーカーを足すと、今度は「今まで吸収されて生きていた文言」が別ブロック扱いになって挙動が変わる。→ §9 の手順で実機から確定させてから。 |
| fix307 の保留（M） | おしんの明示指示「類似している別個体まで強制統合しない」の境界に触れる。§5 の案1〜3を**提案に留めた**。 |
| fix145 の `__isAliasOfRegistered` / `__isVariantOfPeer`（G/H） | 表示専用だが、**直すと今まで隠れていたカードが一斉に出てくる**。「重複が増えた」と見える変化なので、おしんが実機で今の見え方を確認してからの方がよい。 |
| fix135 / fix307 の `scene.loc` 同一性判定（F） | 正しい直し方は「`scene.loc` ではなくスロットIDで一致を見る」だが、これは**longmem とロスターの書込先の決め方を変える**変更で、既存の物語の台帳の帰属が動く。破壊的変更の事前確認が要る。 |

---

## 9. おしんが実機で確認すること

物語画面（`start.html` 経由で最新化してから）の PC ブラウザのコンソールに、上から順に貼る。

### ① 今回入れた3本が生きているか（30秒）

```js
window.__v292Dfix635.selfTest()
window.__v292Dfix636.selfTest()
window.__v292Dfix637.selfTest()
```

- fix635：`?new=1` で開いた物語なら `armed:true, wrapped:true`。普通に開いた物語なら `armed:false`（正常）。
- fix636：`dormant` に**会話ログで喋っているのに休眠している登録NPC**が並んでいたら、それが症状3の当事者。
  `wouldPromote` が空なら、この物語ではもう全員登場扱いになっている。
- fix637：`stateReachable:true` なら状態が取れている。

### ② どの経路が休眠しているかを実測（本命）

```js
window.__v292Dfix637.report()
```

見るところ：

- `deadPaths` の各行の `alive`。**`features.js §8 auto_bootstrap` が `false`** なら §2 の真因が実機で確認できた。
- `heroName` が空で `turns` が3以上なら、症状1の再現状態。
- `proposal` に「本文から取れたはずの主人公名・NPC名」が出る。**この時点では何も書かれていない。**

### ③ `cast.hero.name` が「途中で消えた」のか「最初から空」なのか（宿題Dの決着）

```js
window.__v292Dfix573.log()        // 「あった→無くなった」瞬間の記録。空配列なら一度も消えていない
window.__v292Dfix573.stats()      // emptyAtBoot が true なら起動時点で既に空
window.__v292Dfix573.candidates() // 控えに残っている主人公名
```

- `log()` が**空**なら → 「最初から空」＝ §8 の死が原因。fix637 の `apply()` を検討する段階。
- `log()` に**行がある**なら → 途中で消えている。`stack` に消した犯人の呼び出し元が入っている。**その行をそのまま持ってきてほしい。**

### ④ 症状2「一覧と実際の登場状況の不一致」の内訳

```js
// 画面に出ている「物語に登場」の元データ
window.__longmem.raw.loadWorldInfo().filter(w=>w.type==='character').map(w=>w.name)
// sys へ実際に注入されている準登録（seen>=3 を満たしたものだけ）
window.__v292QuasiPack && Object.entries(JSON.parse(localStorage.getItem('v292Dfix277Quasi'+(window.__chr6Key().replace(/^chr6/,'')))||'{}')).map(([n,e])=>[n,(e.seen||[]).length,e.last])
// fix307 のロスター（LLM が作った呼称の台帳）
JSON.parse(localStorage.getItem('v292Dfix307Roster'+(window.__chr6Key().replace(/^chr6/,'')))||'[]').map(r=>[r.handle,r.firstTurn,r.lastTurn])
```

- 3つのリストの**差**がそのまま症状2。特に3つ目に「一度きりの呼称」が並んでいたら §5 の M が実機で確定する。
- 症状4の「鏡から出る腕／鏡の男／大きな影」が3つ目に3行で並んでいるかを見てほしい。

### ⑤ 症状3「同席しているのに居ないことにされる」（fix636 の効き目）

物語を1ターン進めたあとで：

```js
window.__v292Dfix636.stats()      // promoted が1以上なら、休眠していたNPCを起こした
// モデルへ実際に渡っているキャスト（Planner.build 直後の sys を見る）
window.__v292Dfix459.last()       // sys の字数とブロック数
```

### ⑥ 症状3/全般「sys ブロックが道連れで消えていないか」（§1-P の決着）

Network タブで生成リクエストの `messages[0].content`（system）をコピーして変数 `sys` に入れてから：

```js
// fix459 が認識できているブロックの見出し
window.__v292Dfix459.parse(sys).blocks.map(b=>b.mk)
// sys の中に実在する【…】見出し（先頭が【の行だけ）
sys.split('\n').filter(l=>/^【/.test(l))
```

**2つのリストの差＝fix459 が境界と認識できていない見出し。**
そこに `【キャラ点呼＝消失禁止】` `【場にいる者は"生きている"】` `【話者タグの徹底（最優先）】`
`【準登録キャラ(自動)】` が入っていて、かつ**その直前の見出しが**
`【話者厳守】/【正式呼称】/【呼称の固定】/【whoに使う名前】/【悲鳴・うめきの話者帰属】/`
`【本文形式】/【境界線ルール…】/【出力の掟】/【展開の推進ルール】/【展開を前に進める・優先順位】/`
`【文体の基本ルール】/【描写の作り方…】/【ダッシュ】`
**のどれかだったら、そのブロックは毎ターン道連れで消えている。**その組み合わせを教えてほしい。
（切り分けだけなら `localStorage.v292Dfix459Off='1'` を入れてリロードすると、その瞬間から旧 sys に戻る。）

### ⑦ もし fix637 の提案を採用したくなったら（★書き込みが起きる）

```js
window.__v292Dfix637.proposal()                        // 何が書かれるかを先に見る
localStorage.setItem('v292Dfix637Apply','1')           // 許可
window.__v292Dfix637.apply()                           // 実行（空欄だけ埋める。既存値は上書きしない）
```

戻したいときは設定画面から主人公名を直すか、`window.__v292Dfix573.candidates()` の控えを使う。
**この操作はセーブデータを書き換えるので、③の結論が出るまでは推奨しない。**

---

## 10. この監査で見つけた「次に効く」観測点

- `window.__chronicleState.stats()` … 各fixが `S` を取れているかの実測。`misses` が立っている feature 名がそのまま次の容疑者。
- `window.__v292Dfix637.deadPaths()` … 今回の「休眠モジュール」判定を、**推測ではなく呼び出し結果で**出す。
  今後 `window.S` 型のバグを足してしまったときに、ここに `alive:false` が増える。
