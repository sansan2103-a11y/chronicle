# 設計 — fix640 / fix641：証拠ベースの cast 自動登録

作成 2026-07-29 ／ 対象ブランチ `v292-rebuild` ／ BUILT は触らない（デプロイは別工程）

---

## 0. 何を直すのか

**症状（おしん）**：新しい物語を始めると、本文に何度も出てくる人物が
「キャラ一覧」にも「モデルへ渡るキャスト」にも**永久に登録されない**。

**真因（fix637 で静的に確定済み・実測）**

`S.cast.npcs` へ **人の操作なしに** 書ける経路は features.js §8 `auto_bootstrap` **1つだけ**で、
その §8 は

```js
if (!window.S || !S.cast) return;
```

で始まる。このページの `S` は index.html の**トップレベル `const`** であり、
`window.S` へは**誰も代入していない**（fix539 が正式APIとして `window.__chronicleGetState(feature)` を提供している）。
＝ §8 は **一度も走っていない**。したがって「設定画面を通さずに始めた物語」は
`cast.hero.name` も `cast.npcs` も空のまま何十ターンも進む。

## 0.1 §8 をそのまま起こしてはいけない理由（GPT差し戻し）

§8 は「本文からカタカナ名を拾い、**最初に出た名前を主人公として確定**する」。
おしんの意図と違う名前が `cast.hero.name` に**セーブとして焼き付く**。
`window.S` を新設する案も却下（§8/§9/§10・fix50 など**休眠コード約40箇所**が一斉に目を覚ます）。

---

## 1. 設計方針（GPT裁定・緩めない）

**turn-local presence（fix636）と persistent cast registration（fix640/641）を分離する。**

| 層 | 担当 | 書くもの | 判断 |
|---|---|---|---|
| fix636（既存） | そのターンに**居る**か | `npc.appeared`（既登録NPCのみ） | `_convSays.who` 完全一致 |
| **fix640（新）** | 証拠を**貯める** | **自前キー**のみ（物語データ不触） | 分類するだけ・昇格しない |
| **fix641（新）** | **昇格**させる | `S.cast.npcs` + 由来記録 | 強2系統 × 2ターン |

3層に割るのは「観測 → 判断 → 適用」を分けるこのプロジェクトの既定の型
（fix606→fix611→fix620 と同じ）。fix640 は既定でも**物語データを1バイトも書かない**。

---

## 2. fix640 — 証拠台帳

### 2.1 台帳キー（汚染隔離）

```
v292Dfix640Evid_slot_<slotId>       slotId = __chr6Key() から 'chr6_slot_' を剥いたもの（既定は 'chr6'）
```

- **fix277 の準登録カルテ（`v292Dfix277Quasi*`）は読まない・書かない。**
  fix277 には「他物語のカルテが混ざった」前歴があり、その過去汚染を昇格判定へ持ち込まない（GPT指示）。
- `distinctSeenTurns` は**このキーの中だけ**で数える＝スロットをまたいで合算されない。
- `S.turns.length < cursor`（物語リセット・新規作成）を検出したら台帳を**捨てて作り直す**。

### 2.2 台帳の形

```jsonc
{
  "v": 1,
  "slotId": "sms4np33eyg",
  "cursor": 9,                      // 何ターンまで採取済みか（1ターン1回を保証）
  "entries": {
    "白石澪": {
      "name": "白石澪", "slotId": "sms4np33eyg",
      "firstSeenTurn": 2, "lastTurn": 7,
      "distinctSeenTurns": 4,
      "seenTurns": [2,3,5,7],
      "evidenceKinds": ["say_who", "state_tag", "prose_name"],
      "sourceSpans": [{ "turn": 2, "kind": "say_who", "at": 118, "text": "…" }],
      "roleWord": false, "resolvedTo": "", "resolveCandidates": [],
      "appearance": { "長い黒髪": [2, 5] }
    }
  },
  "promotions": [ /* fix641 が追記する由来。§3.4 */ ],
  "blocked":    [ /* 取り消した名前。再昇格しない */ ]
}
```

### 2.3 証拠の一次ソース

`turn.plan.narrative`（**段落の配列**・実測 165/165 ターン。fix608 の教訓）。
ここに `<say who="…">` が**生きている**。画面用の `turn.narrative` はタグが剥がされている。
抽出規則は fix606 の `partsOf/textOf/evidenceSource` を**live参照**する（無ければ同等の自前実装）。
＝抽出規則の正本を2つ作らない（CLAUDE.md §6）。

### 2.4 証拠の分類

| kind | 強さ | 取り方 |
|---|---|---|
| `say_who` | **強** | `<say who="X">` の who。話者の一次証拠＝最強 |
| `state_tag` | **強** | `<state who="X" …>` の who。モデルが「継続追跡すべき人物」と宣言した印 |
| `introduction` | **強** | 明示的な人物紹介：`Xと申します` / `名は X` / `Xと名乗った` / `名前はX` / `Xという名の` |
| `appearance_stable` | **強** | 同じ外見トークン（修飾＋外見名詞、例「長い黒髪」）が**別々の2ターン**で名前の近傍に出た |
| `prose_name` | 弱 | 地の文に名前が出た |
| `react` | 弱 | `<react who="X">`（反応しただけ） |
| `recall` | 弱 | 回想マーカ（回想/思い出/かつて/昔/あの日/記憶/写真/夢/噂/伝説）と同じ段落での言及 |
| `role_word` | 弱 | 役割語そのもの（「宿の主人」「少女」…） |

> **`<state who>` を強にした根拠**：モデル出力プロトコルに「登録キャスト追加タグ」は**存在しない**
> （`<say>` `<state>` `<react>` の3つだけ・実配信を全数確認）。GPTの言う「登録キャスト追加タグ」に
> 最も近いのは、fix77 が人物カルテとして収穫している `<state who>` である。推測で新タグを作らない。

> **`_convSays[].who` は強証拠にしない**。あれは fix620 が後から**書き換えられる**派生値なので、
> 昇格の根拠にすると「推測のロンダリング」になる。fix636（turn-local）と同じ強さでは使わない。

### 2.5 回想の扱い

回想段落だけで言及されたターンは `distinctSeenTurns` に**数えない**
（fix408 が「回想の中にだけ出る存在が登録された」実害を踏んでいる）。

### 2.6 役割語

- 判定：(a) 閉じた語彙集合（少女/少年/男/女/老人/主人/店主/女将/番頭/客/怪異/人影…）
  (b) `…の(主人|店主|女将|少女|少年|男|女|客|者|声|影|…)$`（→「宿の主人」「白鷺荘の主人」）
  (c) `若い/年老いた/背の高い…` ＋ 役割名詞。
- `隼人` `麻子` のような**実名の誤検出を避ける**ため、(b) は直前に「の」を必須にする。
- 役割語エントリは `roleWord:true`。正式名へ**一意に**解決できた場合だけ `resolvedTo` が入る。
  解決パターン：`役割語の◯◯` / `役割語、◯◯` / `役割語は◯◯と名乗った` / `◯◯という役割語`。
  **候補が2つ以上出たら曖昧＝永久に解決しない**（`resolvedTo` は空のまま）。

### 2.7 いつ走るか（軽さ）

- `UI.appendTurn` ラップ（＝ターンが確定した瞬間。fix616 と同じ観測点）で**そのターンだけ**採取。
- 起動時に1回、`cursor` から `turns.length` までを追いつき採取（既存の物語のため）。
- `UI._renderHooks` にも追いつき用の**カーソル比較だけ**を置く（未採取が無ければ即 return）。
- **1ターンは必ず1回だけ**走査される（cursor が保証）。59秒スイープのような周期処理は置かない。

---

## 3. fix641 — 昇格

### 3.1 判定（擬似コード）

```
for each entry E in ledger.entries:
    if OFF('v292Dfix641Off')            -> stop（新規昇格を全部止める）
    if E.name == cast.hero.name         -> skip 'hero'      # 主人公は人の操作のみ
    if E.name in ledger.blocked         -> skip 'blocked'   # 取り消し済み
    target = E.roleWord ? E.resolvedTo : E.name
    if target == ''                     -> skip 'role-word-unresolved'
    if E.distinctSeenTurns < 2          -> skip 'few-turns'
    strong = unique{ k in E.evidenceKinds | isStrong(k) }
    if strong.length < 2                -> skip 'weak-evidence'   # 弱が3回でも通らない
    if target が既存キャストと一致       -> skip 'already'         # 完全一致 + 既存の正規化関数
    S.cast.npcs.push({ name: target, desc: '', appeared: true,
                       autoBy: 'fix641', autoAt: now })
    ledger.promotions.push({ name: target, from: E.name, turn: E.lastTurn,
                             kinds: strong, spans: E.sourceSpans, ts: now })
    S.save()
```

- **`appeared: true` を付ける根拠**：fix95 は `npc.appeared !== true` のNPCを
  sys から**外す**。証拠上そのターンに居た人物を登録直後に休眠させると、
  「登録したのにモデルへ渡らない」（＝fix636 が直したのと同じ穴）に自分で落ちる。
- **類似統合はしない**（おしんの明示方針）。重複判定は完全一致と、
  既にある正規化関数（fix197 `canonName` / fix445 `castMatch` を live参照）だけ。無ければ完全一致のみ。

### 3.2 主人公を埋めない

`cast.hero.name` へは**絶対に書かない**。hero と同名の候補は昇格対象から外す
（hero が空のときは「hero と同名」判定が発生しないので、hero 名の候補も普通のNPCとして扱われうる
＝これは仕様。hero 欄は人の操作でしか埋まらない）。

### 3.3 取り消し

| 口 | 効果 |
|---|---|
| `localStorage.v292Dfix641Off='1'` | **新規昇格を止める**（既に入ったものは消さない） |
| `__v292Dfix641.undo('名前')` | その1件を `S.cast.npcs` から外し、`blocked` に入れて再昇格を止める |
| `__v292Dfix641.undoAll()` | fix641 が入れたもの全部 |
| `__v292Dfix641.unblock('名前')` | 再昇格を許す |

`undo` は **`autoBy === 'fix641'` の項目しか消さない**（人が登録したNPCは絶対に消さない）。

### 3.4 由来

`ledger.promotions[]` に `{name, from, turn, kinds, spans, ts}` を残す。
`__v292Dfix641.why('名前')` でその1件を引ける。

---

## 4. 書いた後に載るか（2経路の検証観点）

「モデルへ渡る側と画面に出る側は別経路」というこのプロジェクトの教訓に従い、**両方**を確かめる。

| 経路 | 実体 | 確認方法 |
|---|---|---|
| (a) 画面 = キャラ一覧 | fix145 が `S.cast.npcs` を直接読んで描画 | 一覧を開き直して行が増えているか |
| (b) モデル = sys注入 | features.js fix95 が `appeared===true` のNPCだけを残して `Planner.build` へ渡す。fix50 の声紋ブロック / fix456 の【whoに使う名前】も `S.cast` を読む | `Planner.build('DO','')` の戻り `.sys` に名前が入るか。実機では Network 傍受で実sysを見る |

`appeared:true` を付けているので (b) は同ターンから有効。
なお fix50 `getRoster()` と features.js §9 は **`window.S` 依存で休眠中**なので、
そこには載らない（fix637 `deadPaths()` が既に報告している既知の休眠。今回の対象外）。

---

## 5. やらないこと

- `window.S` を新設しない（休眠コード約40箇所を起こす）。
- features.js / fix277 / fix307 を1バイトも変更しない。
- 準登録カルテ（fix277）の過去データを読まない。
- 名前の類似統合・別名マージをしない。
- `cast.hero` を埋めない。`_convSays` / `who` / 本文 / DOM を書き換えない。
- BUILT / version.txt を触らない。デプロイしない。

---

## 5.1 実装中に踏んで直したこと（推測ではなく実測）

| 症状 | 直し方 |
|---|---|
| `<state>` の後ろに続く地の文を丸ごと捨てていた（「最初の `<state>` から後ろを切る」方式） | 管理タグを**要素単位**で落とす。段落順に依存しない |
| `宿の主人の佐々木が帳場から顔を上げ` を丸ごと名前として拾った | 名前として拾う文字からひらがなを外す（助詞で自然に止まる）。ひらがなだけの名前は拾えなくなるが、**拾い過ぎるより拾わない方**を選ぶ |
| 「ミナの長い黒髪」と「長い黒髪の女」が別トークンになり、外見の一貫性を永久に検出できなかった | 修飾語の直前の「の」までを落として正規化 |
| 「宿の主人」から「主人」「主」まで別エントリが増えた | 既に採った長い役割語の一部でしかない語は別エントリにしない |
| 描画のたびに台帳を `JSON.parse` していた | 同一スロットでターン数が変わっていなければ localStorage を触らない門番を置いた |

`test_fix547.cjs` の「正式API(fix539)を使うファイル数」台帳を 31 → 33 に更新した
（fix640/fix641 が正式API経由で S を取るため。この台帳は番号ではなく
「新しく S を読むモジュールは必ず正式API経由にする」という契約を固定している）。

## 5.2 白鷺荘（slot `sms4np33eyg`・9ターン）での予想

同型の9ターンを組んで実チェーンで流した結果（実測）:

| 候補 | ターン | 強い証拠 | 結果 |
|---|---|---|---|
| 宿の主人 | 4 | say_who, state_tag | **昇格しない**（`role-word-unresolved`） |
| 仲居 | 2 | say_who | 昇格しない（役割語＋強1系統） |
| 千鶴 | 3 | say_who, state_tag, introduction | **昇格する** |

「宿の主人」は強い証拠が2系統・4ターン揃っていても、**正式名を明かさない限り登録されない**。
本文で一度でも「宿の主人の◯◯」と名前が出れば、その**正式名**で登録される。

## 6. 回帰テストで固定する契約

`test_fix640.cjs` / `test_fix641.cjs`（実チェーン = 配信JSをそのまま mock window 上で実行）

1. 強い証拠2系統 × 別々の2ターン → **昇格する**
2. 弱い証拠が3回あっても → **昇格しない**
3. 役割語（「宿の主人」）は → **昇格しない**（一意解決できたら正式名で昇格）
4. `cast.hero` を**埋めない**／hero と同名は昇格しない
5. 既存キャストと**重複登録しない**
6. `v292Dfix641Off='1'` で新規昇格が止まる／`undo` で外せる
7. fix277 の台帳キー（`v292Dfix277Quasi*`）に**触れない**（読みも書きもしない）
8. fix640 は**物語データを1バイトも書かない**／1ターンを2回走査しない
9. OFF・冪等・index.html 配線・NUL 1個
