# おしんの手順書（これ1枚で完結します）

2026-07-26 時点 / 本番 **20260726-fix587**

このセッションに戻ってこなくても済むように、**やること・困ったときの対処・確認方法**を全部ここに書きました。
上から順に読めば大丈夫です。専門用語は使っていません。

---

## 1. いますぐやること（1つだけ・1〜2分）

### Cloudflare の Worker を貼り替える

**急ぎではありません。** 手が空いたときで大丈夫です。やらなくても今は何も壊れません。

1. Cloudflare のダッシュボード → **Workers & Pages** → **novel-proxy**
2. 右上の **Edit code**（クイック編集）
3. エディタ内で全選択（⌘+A / Ctrl+A）して、**`chronicle-proxy-v24_tombstone.js`** の中身をまるごと貼り付け
4. **Save and Deploy**

これだけです。設定値も環境変数もいじりません。

**元に戻したいとき**: 同じ手順で `chronicle-proxy-v23b-imgsync.js` を貼り直すだけ。

**何のため？**
物語を削除したとき、古いキャッシュを持った端末が「削除した」記録をうっかり消すのを防ぐ保険です。

---

## 2. 特別なことをしなくていいこと

### PC と iPhone の両方で普通に遊ぶ

それだけです。**確認作業は要りません。**
両方で遊んだあとに数字を見れば、同期がぶつかったときの動きが分かります。

---

## 3. 何か変になったら（緊急停止）

**まず落ち着いてください。今日入れた仕組みは全部「迷ったら消さない」側に倒してあります。**
勝手にデータが消えることは、以前より起こりにくくなっています。

打ち方: ブラウザで Chronicle を開き、開発者コンソール（F12 → Console）に貼って Enter。
**打ったあとページを再読み込み**してください。

### まず試す（止めても危険は増えません）

| 症状 | 打つもの |
|---|---|
| 物語が削除できない・削除でおかしくなった | `localStorage.v292Dfix587Off='1'` |
| ギャラリーの削除ボタンで困った | `localStorage.v292Dfix577Off='1'` |
| セーブの保存自体が失敗する | `localStorage.v292Dfix576Off='1'` |
| クラウド取り込み（pull）で止まる | `localStorage.v292Dfix575Off='1'` |

ここまでは、止めると**その機能が使えなくなるだけ**です。データが危なくなることはありません。

### ★同期がどうしても直らないときだけ（**注意あり**）

| 打つもの | 何が起きるか |
|---|---|
| `localStorage.v292Dfix582Off='1'` | 送信が**今日直す前の挙動**に戻ります |
| `localStorage.v292Dfix584Off='1'` | 同上 |

**★これは「他の端末のセーブを確認せずに上書きする」古い動きに戻します。**
PCとiPhoneの両方で遊んでいる状態でこれを使うと、**片方のセーブが消える可能性があります**。

使うなら、**1台だけで使う・使ったらすぐ戻す**。戻し方:

```js
localStorage.removeItem('v292Dfix582Off'); localStorage.removeItem('v292Dfix584Off');
```

### 全部止める（本当の最終手段）

```js
['587','577','576','575','573'].forEach(n => localStorage['v292Dfix'+n+'Off']='1');
```

**同期の2つ（582/584）は、上の注意を読んでから別途**にしてください。
また `564`（復元セット）と `562`（分類）と `569`（削除の窓口）は**止めないでください**。
止めると**消した物語を戻せなくなります**。

### 元に戻す

```js
['587','584','582','580','579','577','576','575','573','569','564','562']
  .forEach(n => localStorage.removeItem('v292Dfix'+n+'Off'));
```

---

## 4. 消した物語を戻したい

削除するときに**復元セット**（本体＋記憶や登場人物などを一組にしたもの）を必ず作っています。
戻せます。

### 手順

コンソールで、まず何があるか見る:

```js
__v292Dfix564.list()
```

`chr6_snap_<物語ID>_<数字>` のような名前が並びます。戻したいものを選んで、**まず下見**:

```js
__v292Dfix564.restore('ここに名前を貼る')
```

これは**まだ書き戻しません**。何を上書きするかだけ教えてくれます。
内容を見て良ければ、本番:

```js
__v292Dfix564.restore('ここに名前を貼る', { confirm: true })
```

**注意**: 照合が通らない復元セットは、そもそも復元されません（安全側）。

---

## 5. 様子を見たいとき（任意）

コンソールで打つと、日本語で状況が出ます。**打たなくても大丈夫です。**

```js
__v292Dfix580.report()        // 同期の状況（送信回数・競合の有無）
__chronicleStoryLifecycle.stats()   // 削除の状況
__v292Dfix562.report()        // 保存容量の内訳
```

---

## 6. 私に伝えてもらえると助かること

もし何か起きたら、下をコピーして貼ってもらえれば原因がすぐ追えます。

```js
JSON.stringify({
  build: document.documentElement.outerHTML.match(/20260726-fix\d+/)?.[0],
  sync: __v292Dfix580.stats(),
  del: __chronicleStoryLifecycle.stats(),
  delLog: __chronicleStoryLifecycle.log(),
  gate: __v292Dfix569.stats().gate,
  keys: localStorage.length
})
```

---

## 7. いま分かっている「まだ直っていないこと」

正直に書いておきます。どれも**今すぐ困るものではありません**。

- **主人公の名前が空になることがある** … 原因が特定できていません。
  検知して控えを取り、警告を出す仕組み（fix573）は入っていますが、**あえて自動修復していません**。
  原因が分からないまま直すと、別の壊し方をするためです。もし起きたら教えてください。
- **画像データが容量を食っている** … 名前をキーに全物語で共有される作りで、
  どの物語のものか判別できません。**消えはしません**が、整理もできない状態です。次の課題です。
- **セーブ数の上限** … まだありません。ホーム画面を管理画面にする回で、
  上限で縛るのではなく「何がどれだけ容量を食っているか見える」形にする予定です。

---

## 8. 今日やったことの要約（読まなくても大丈夫）

- 消したはずの物語が**クラウドから復活する**問題を根治しました
- 削除の入口が3つあってバラバラだったのを**1つに統合**しました
- 削除前に**本体＋関連データを一組で退避**し、照合が通らなければ消さないようにしました
- クラウド同期で**他の端末のセーブを黙って上書きする**状態を止めました
- 「消す」処理を**1つの窓口**に通し、守ると決めたデータは通さないようにしました

テストは34ファイル・1379件、全部通っています。
