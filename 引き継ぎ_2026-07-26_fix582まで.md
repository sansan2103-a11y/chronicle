# Chronicle 引き継ぎ — 2026-07-26 / fix582 まで

本番: **20260726-fix582**（Pages反映済み・実機検証済み）
テスト: **31ファイル / 1311件 / 失敗0**（`node run_all_tests.cjs` が正本）

---

## 1. このセッションで出荷したもの（fix575 → fix578）

| build | 内容 |
|---|---|
| fix575 | fix399の容量回復削除を fix569 の exact-delete ゲートへ。`while`→単発再試行 |
| fix576 | ゲート未搭載を旧経路へ自動フォールバックさせない ＋ A2(fix490Quota) |
| fix577 | **削除入口を1つに寄せる**（B/Cの独自削除を停止）＋ `chr6_bk_del_` のsortバグ |
| fix578 | **A3.1** fix562に `classifyKey()` / `deletePolicy()`（読取専用）＋ 削除控えを「書けてから整理」へ |
| fix579 | **A3.2** 画像を `shared-asset` へ分類 ／ 壊れた生セーブも hard 維持 ／ **tombstoneスキーマ（未配線）** ／ `Object.keys(localStorage)` の静的検査 |
| fix580 | **A3.3-1** 同期の中央調停役 `MetaSyncCoordinator` を**影モード（観測のみ）**で追加 ／ mergeMetaの可換・結合・冪等テスト |
| fix581 | fix580 を **fetchラッパ21本の先頭**へ移動（後ろだと先行5本に迂回されていた） |
| fix581b | fix580 の**キャッシュバスター(cb)を上げる**（旧版JSがキャッシュされ、実機で新しい計測値が出なかった） |
| fix582 | **A3.4-5** fix399 の push を**サーバの競合検査(CAS)へ参加**させる。baseRev を共有し、fork時は fail-closed |

### fix582 の要点

- fix399 の put に **baseRev** を載せた。正本は `localStorage['v292Dfix580_rev']`（Coordinatorが持つ）。
- **成功応答のrevだけ**を昇格。fork応答のrevは「サーバの現在値」であって自分の書込みが通った証拠ではないので昇格しない。
- rev は**巻き戻さない**（古い応答が遅れて届いても下げない）。
- 初回は **fix402 の既存 baseRev を引き継ぐ**。0から始めると全端末がforkまみれになる。
  → 実機で **rev=415 を fix402 から引き継ぎ確認済み**。
- `serverTs`/`baseTs` は書込み認可から外し、`baseTs_diagnosticOnly` へ改名して診断値へ降格。
- 緊急停止 `v292Dfix582Off='1'` で旧挙動へ。

#### ★壊れ方の向きが変わった（この変更の意味）
| | 変更前 | 変更後 |
|---|---|---|
| サーバの競合検査 | **参加していない**（無条件上書き） | 参加する |
| 競合したとき | **別端末のセーブが消える** | fork（サーバが両方保持）→ pushが本流に入らないだけ |

後者は気づけるし戻せる。前者は戻せない。

#### ★ご指定と1点違う実装（GPTへ裁定依頼済み・未回答）
GPT指定は「fork時は再取得・再マージして1回だけ再試行」。しかし実装中に**設計バグを踏んだ**:

> push直前にサーバの現在revを自分の baseRev として採用すると、**必ず一致して fork が起きない**。
> それは「サーバに何が入っていようが自分で上書きする」＝いま無くそうとしている無条件上書きそのもの。
> **baseRev は「自分のローカル状態が derive された版」でなければ意味がない。**

同じ理由で「fork後にサーバの現在revを取り直して押し直す」も、相手の中身を確認せず塗り替えることになる。
全状態のmerge関数がまだ無いので『再マージ』を正しく実行できない。そこで:

- 再試行するのは**自端末の別経路(fix402)が同期中にrevを進めた場合だけ**
  （push開始時の共有revと、fork応答時の共有revが違う＝同端末の同時発火）
- それ以外（別端末との本物の分岐）は**再試行せず fail-closed**。dirtyを残し `serverRev` を例外に載せる

全状態mergeが入る段（A3.5以降）で本来の再マージ再試行へ格上げする想定。

### ★fix580/581 で判明した重要な事実

**fix399 の push は、サーバから見ると「無条件上書き」。**

Worker(`worker/chronicle-proxy-v23b-imgsync.js:1501`) は
`hasBase = body.baseRev !== undefined && !== null` で分岐し、
**hasBase が false なら fork判定を一切しない**（無条件上書きの経路へ進む）。

| 経路 | 送っているもの | サーバのCAS |
|---|---|---|
| fix402 | `callSave({op:'put', baseRev: baseRev(), ...})` | **参加している** |
| fix399 | `callSave({op:'put', pkg})` | **参加していない**（保護はクライアント側の時刻比較 `serverTs > baseTs()` だけ） |

→ 墓標を meta に載せても、**この経路が残る限り旧metaで踏み潰される**。
→ ★Worker側の rev/baseRev 楽観ロックは**既に実装済み**（不一致は `saveIncomingAsFork` で両方保持・絶対に上書きしない）。
  足りないのは**サーバではなくクライアント側**。

さらに: fix399 と fix402 が**別々の基準をlocalStorageに持っている**。
`v292Dfix399_baseTs`（時刻） vs `v292Dfix402_baseRev`（版番号）。統一が要る。

### fix579 の要点

- **画像・外見系を `shared-asset` へ**（`chrAiAv*:` / `v292av*_` / `v292avrec_`）。
  スロットIDを持たず**名前キーで全スロット共有**なので `slotId:null`、`protection:'protected'`。
  参照カウントと削除は**まだ実装しない**（GPT指定）。
  → 実機効果: **review が 398件/295KB → 157件/238KB**、protected が 48→289件/1003KB。
- **壊れたJSONでも生セーブは hard のまま**。`completeness:'ok'|'broken'|'missing'` を追加。
  「壊れているから消してよい」への降格をしない（壊れた本体こそ手作業復元の対象）。
- **`Object.keys(localStorage)` の禁止＋静的検査**。書いたら実際に2件残っていた:
  `v292Dfix197 clearCache()` と `v292Dfix277`（fix538控えの3世代整理）。
  どちらも `length`+`key(i)` へ。**列挙してから消す**（列挙中に消すと添字がずれる）。
  fix277側の世代sortも末尾13桁の数値比較へ修正。
  ※検査は**コメントを除去してから**走らせる（除去しないと説明コメント自身が引っかかる。実際に踏んだ）
- **`v292Dfix579-tombstone-schema.js`（新規）** … 純粋関数のみ。**localStorageを触らない・fetchしない・
  どこからも呼ばれていない**（`wiredIntoSync:false`）。挙動は1バイトも変わらない。
  `make/validate`（deleteOpId必須）/ `visible`（T1）/ `mergeMeta`（T3-T6・**対称**）/ `isBlockedByTombstone`（T2）。
  ★mergeMeta を対称にしたのは、非対称だとPC視点とiPhone視点で結果が食い違い、
    同期のたびに墓標が立ったり消えたりする振動を作るため。

### fix577 の要点 — 物語削除の入口が3系統あった

棚卸しで判明した事実:

| 経路 | 消す範囲 | 問題 |
|---|---|---|
| A `home.html:delStory()` | 本体＋メタ＋idの部分一致でサイドストア | 現行で唯一まともな経路 |
| B `fix310:deleteSave()` | 掃き出しが `'_slot_'+id` 固定 | **引用符付きキーを取りこぼす**／控えなし |
| C `features.js:clearSlot()` | 本体1キーのみ | **metaにエントリを残す**→fix402cがdoomedにせず**他端末から復活** |

→ 新規 `v292Dfix577-delete-entry-guard.js` を追加。B/Cは
`window.__v292Dfix577.requestDelete(slotId,{source})` を呼ぶだけにし、自前削除を全廃。
正規サービス未搭載なら**削除せず fail-closed**（旧実装へは戻さない）。
`v292Dfix577Off='1'` は「委譲を停止して安全に拒否する」の意味で、**旧削除経路の復活スイッチではない**。

### fix578 の要点 — 分類器（A3.1/A3.2）

- `classifyKey(key)` → `{family, slotId, protection, owner, why}`
  保護は **hard / protected / releasable / review** の4段。**形式不明(review)は削除許可しない**。
- `deletePolicy({key, intent, path})` → `{allow, code, classification}`。**物理削除しない**。
  - 生セーブへの `reclaim`/`retention` → 常に `allow:false` / `live-data-requires-lifecycle-authorization`
  - `lifecycle-delete` → tombstone barrier 未実装なので `lifecycle-delete-not-ready`
  - barrier 実装後も計画検証前は `lifecycle-delete-requires-verified-plan`
- **`protectedSet()` は1バイトも変えていない**（A3.2の不変テストを test_fix578.cjs に同梱）

---

## 2. 実機で確認した数字（fix578・実データ563キー・読取専用）

```
selfTest ok:true / isOutermost:true / wrapperErrors:0 / classifierErrors:0 / denied:0
主人公=白石澪 / 実スロット10件健在（+ ab552p1A は診断用の残骸）
分類結果:  hard 109件/562KB ・ protected 48件/946KB ・ releasable 8件/155KB ・ review 398件/295KB
★引用符付きキー chr6_v292Dfix54_genderMap_"<id>" は実データに 14件 実在。slotId を正しく抽出し hard になる
```

fix577 のガードも実データで確認: `refused` / **キー数563→563で1件も消えず** / 物語は残存 / 案内メッセージ表示。

---

## 3. ★構造的な欠陥（未解決・A3の前提条件）

**ローカルで削除した物語は、次の bootPull でクラウドから復活しうる。**

- tombstone/墓標の仕組みは**存在しない**（全js/htmlをgrepして0件）
- `applySave` は `setItem` のみの純マージ。クラウドの `chr6_slot_*` は**無条件に書き戻される**
- push の起動点は `S.save` のラップだけ。削除は `S.save`/`markDirty` を呼ばない → **dirtyにならない**
- 削除伝播は fix402c の「サーバmetaに無いローカルslotを消す」= **cloud→localの一方向のみ**

→ GPT裁定: **これはA3の後回し課題ではなく、A3の前提条件**。
  tombstone または pull側barrierが無いまま生セーブの `lifecycle-delete` を許可してはいけない。
  fix578 の `deletePolicy` は現にそう実装してある（`lifecycle-delete-not-ready`）。

---

## 4. 次にやること — A3の残り（GPT裁定の順序）

**採用方式は「案3′」**: tombstone = 削除の正本 / **fix564の完全スナップショットを復元セットに再利用**（新しい退避方式は作らない）/ DeleteGateway = 唯一の物理削除者。

```
A3.1 classifyKey()                      ← 完了(fix578) / shared-asset対応(fix579)
A3.2 protectedSet()不変テスト＋tombstoneスキーマ ← 完了(fix578/fix579)
GPT指定の10段:
  1. MetaSyncCoordinator を追加（影モード）          ← 完了(fix580/581)
  2. fix399／fix402のpush要求を記録し二重発火を測定   ← 完了(fix580)。実測はプレイ中に貯まる
  3. mergeMetaの可換・結合・冪等テスト               ← 完了(fix580)
  4. D1のrev条件付き書込みを実装                     ← 完了(fix582)
  5. conflict時の bounded retry                     ← 完了(fix582・ただし上記の1点は裁定待ち)
  6. サーバ側 tombstone保持規則（通常pushでは解除できない）  ← 次はここ
     ★「墓標ありの既存レコードは baseRev なし put で上書きできない」最終防御。
       古いクライアントから墓標を守るために、tombstoneを同期へ配線する**前**に必要（GPT指定）。
       Worker は worker/chronicle-proxy-v23b-imgsync.js（リポジトリ内にソースあり）
  7. fix399／fix402 を中央同期層へ接続
  8. 実機 PC↔iPhone↔PC の競合試験
  9. pull側 tombstone barrier
 10. 成功確認後に物理削除計画を解禁
A3.4 fix564 snapshot を trash-recovery へ対応
A3.5 StoryLifecycleService（snapshot→tombstone→push確認→削除計画）＋ ★正式な非モーダル通知口
A3.6 DeleteGateway で exact key を物理削除
A3.7 A/B/C の全入口を StoryLifecycleService へ委譲
A3.8 復元処理: restoreOfDeleteOpId 一致時だけ tombstone 解除
A3.9 trash snapshot を中央GCへ接続
```

### ★A3.3 の注意（GPT名指し）
> pull側だけ先にbarrierを入れても、pushでtombstoneが古いmetaに消されるなら完全ではありません。
> **push競合規則とpull barrierは一組として扱ってください。**

安全に成立する条件はどちらか:
- **条件A**: サーバまたは同期層が tombstone をマージ（どちらか一方に `deleted:true` があれば通常の live meta では解除できない）
- **条件B**: push前に最新metaを取得 → tombstoneをローカルmetaへ合成 → その後push

### 削除処理の正しい順序（A3.5）
```
①現在のスロットを再読込 ②本体＋サイドストアのexact key一覧とhashを確定
③fix564で完全スナップショットを作成 ④全パーツread-back・hash一致・complete:true を確認
⑤metaにtombstoneを保存 ⑥tombstoneをクラウドへpushし、反映確認
⑦DeleteGatewayへ物理削除計画を渡す ⑧本体＋サイドストアをexact-delete ⑨一覧から非表示
```
**物理削除より先に tombstone をクラウドへ確定させることが要。**
オフライン/push失敗時は「tombstoneをローカル保存→一覧から非表示→**物理削除は保留**→再接続後にpush成功→その後で物理削除」。

### meta tombstone の形
```js
{ id:'smrg85jwsn6', title:'湾の鵬野学園', deleted:true, deletedAt:1785070000000,
  deleteOpId:'del_smrg85jwsn6_xxxxx', recoverySnapshotId:'snap_xxxxx', lifecycleVersion:1 }
```
不変条件 T1〜T6:
1. `deleted:true` のスロットは通常一覧へ出さない
2. `deleted:true` のスロットに対するクラウド上の古い本体は、ローカルへ書き戻さない
3. 本体データが存在していても、tombstoneがあれば削除状態を優先
4. 通常のpushはtombstoneを消せない
5. tombstoneを解除できるのは、明示的な復元処理だけ
6. 復元は `restoreOfDeleteOpId` が現在の `deleteOpId` と一致した場合だけ許可

### 認可（confirmは認可ではない）
発行者は UI でも DeleteGateway でもなく **StoryLifecycleService**。UIは意思表示を受け取るだけ。
```js
authorization: { planId, deleteOpId, slotId, snapshotId, allowedKeysHash, createdAt, policyVersion }
```
ゲート側で確認する7条件: slotId一致 / exact keyが計画の許可集合内 / 計画後にhash不変 /
intentがlifecycle-delete / 認可元がallowlist内 / 計画が期限切れでない / 同じ計画で未認可キーを消さない。
**生セーブ削除では、同じスロットのサイドストアを論理単位として扱う。**

---

## 5. ★未確認・未着手（重要）

### put の実測がまだ0件
fix580 は capturedNativeFetch:true で観測できる状態だが、**実際の push はクラウドへの書込み**なので、
おしん不在中に判断で発火させていない。次にプレイして同期が走れば baseRev の実値と source分類が記録される。
GPTからは「active配線前か直後に最低限 fix399由来put 1回 / fix402由来put 1回 を実ブラウザで通せ」と指定あり。
**おしんが同期を1回走らせた直後に `__v292Dfix580.report()` を見るのが最短。**

### 受け入れ条件で未達のもの
- 「同時発火でも並行putは最大1本」… 既存の `pushing` フラグで担保しているがテスト未追加
- 「tombstoneのあるD1レコードへ baseRev なし put → 上書きされない」… **未着手**（実装順6）

## 6. ★GPTへ投げて未回答の論点（1件）

**実装順4の解釈確認**。Worker側の楽観ロックは既に実装済みなので、やるべきは
「Workerを直す」ではなく「fix399に baseRev を持たせる＋fork時のbounded retry」だと理解している。
あわせて、fix399(`_baseTs`＝時刻) と fix402(`_baseRev`＝版番号) の**どちらを正本にするか**。

### 既に得ているGPT裁定（未回答ではない）
- **A**: fix399とfix402の両方を、1つの `MetaSyncCoordinator` へ接続。個別にmeta合成・pushさせない。
- **B**: **revによる条件付き書込みまで実装**。「次回収束する」は削除には不十分。
  CAS競合時は**再取得・再マージを1回**行い、それでも競合すれば **fail-closed**。
- さらに、古いクライアントから墓標を守るため、**D1側でも通常pushではtombstoneを解除できない規則**が必要。

### 残った review 157件の中身（もう画像ではない）
`v292Dfix<N>OnV1` / `v292Dfix<N>Off` などの**機能フラグ**が最多。
他に `v292en_<hash>`、`__f487bk_chrAiAv4:...`（fix487の画像控え）、`chr6_cleanup_log_<ts>`。
フラグ類は review のまま（自動削除しない）が妥当と考えている。

---

## 7. その他の残タスク

- **B**: 第2段（画像の変更検知をキー名→内容hash・原因A/C）／第3段（`localAvKeys()`/`localAv()` → IDB・原因B）／`chrAiAv4:` を同期対象に追加（原因D'）
- **C**: `cast.hero.name` が空になる根本原因は**未特定**（再現せず、fix573が検知・控え・警告のみ）／fix572後に `__gen_` 世代が再生成されるか確認／診断ログ・`__v346raw`・`ab552p1A` の掃除
- **D**: 1MBソフト上限の自動GC／第4段 ホーム画面の管理画面化（**「作成上限」の話はここ。ハード上限ではなく可視化として**）／第5段 セット単位の復元
- **E**: surroundingChars診断／休眠ガード40件の仕分け／`collectIdentityEvidence`／別名の推移解決／rosterのターン番号異常／490文字ケース／同期の冪等キー

---

## 8. 環境まわりの地雷（毎回踏むので必ず読む）

- `index.html` に **NULバイトが1個**ある。編集は必ず Python の bytes で。`home.html` は0個。
- **git push は通らない**（`GH_TOKEN` は14文字の無効値）。出荷は **GitHub Web の Upload files** から。
  出荷後はコンテナ側で `git fetch && git reset --hard FETCH_HEAD`。
- Pages反映は `version.txt?cb=` を数回ポーリング（毎回2回目で反映している）。
- **`?story=<id>` を付けずに index.html を開くと bootPull が走り、削除済み物語が D1 から復活する**。
  検証は必ず `index.html?v=<build>&story=smrg85jwsn6` で。
- モックの localStorage は `Object.keys()` でキーが見えるようにすること（`Object.defineProperty` で各キーを露出）。
  忘れると fix562 のような走査コードが「何も無い」と判断して素通りする。**2回踏んだ**。
- **テスト内でビルド番号を直に固定しない**（出荷のたびに落ちる）。「fix577以降」のような比較にする。
- ChatGPT の長文は `window.__ans.slice()` だと内容依存でブロックされる。**スクリーンショットで読む**。
  送信ボタンは ref クリックが効かないことがある。**座標(1181,656)のクリックが確実**。
- 実機で `window.showToast` は**グローバルに存在しない**（features.jsのクロージャ内）。
  fix577の通知は実際には `alert()` 経路。A3.5で正式な非モーダル通知口を作ること。
- 実機プローブで `alert()` を踏むと拡張が固まる。**プローブ中は alert を差し替える**。
- ChatGPTの送信ボタンは**1回のクリックで送信されないことが多い**。送信後は必ず
  `#prompt-textarea` の `textContent.length` が0になったかを確認し、残っていたらもう一度クリックする。
- 実機プローブで `JSON.stringify` する式は **IIFE で包む**。包まないと最後の評価値が window になり
  「Converting circular structure to JSON」で落ちる。
- 静的検査を書くときは**コメントを除去してから**走らせる。除去しないと、
  その禁止事項を説明したコメント自身が引っかかり、正しく直したファイルが永久に赤くなる。
- ★**JSを直したら index.html の `?cb=` も必ず上げる**。上げないとブラウザが旧版を使い続け、
  「出荷したのに実機で変わらない」になる（2026-07-26に踏んだ。test_fix580.cjs に検査を入れた）。
- ★**`window.fetch` をラップしているファイルは実測21本**ある。fetch境界で観測するfixは
  **fix569の次（script #1）**に置くこと。後ろに置くと先行5本（fix553/fix247/features.js/fix80/fix84）に迂回される。
  `capturedNativeFetch` が true かで実機確認できる。
- ★★`git reset --hard FETCH_HEAD` は**未コミットの作業を消す**。**2回踏んだ**。
  手順を固定する: ①GitHub Webでコミット → ②`git fetch` → ③**`git log FETCH_HEAD` に目的のコミットが
  出ていることを確認してから** ④はじめて `git reset --hard`。
  出荷用の控え `/mnt/user-data/outputs/ship-*` を消さずに残しておくこと（実際にそこから2回復元した）。
- ChatGPTの送信は**クリックが効かないことが多い**。`#prompt-textarea` に focus してから
  **Enterキー**が確実。送信後は必ず textContent.length が0になったか確認する。
