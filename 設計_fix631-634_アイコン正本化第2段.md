# 設計 — fix631〜634「アイコン正本化 第2段」

作成: 2026-07-29 / 対象ブランチ: `v292-rebuild`(HEAD=fix630) / **この文書の時点でデプロイはしていない**

---

## 0. 結論（先に）

アイコンの「作り直しても他端末に届かない／別端末で別の絵に化ける」は、**4つの独立した欠陥が同時に成立している**ために起きている。
4つとも「新規パッチ1ファイル＋既存ファイル無改造」で塞げる。**Worker(v26)の変更は不要**（既存 `op:putimg` / `op:imgmanifest` / `GET /img` の3つだけで足りる）。

| 原因 | 実体 | 修正 | 種別 |
|---|---|---|---|
| A | 画像の変更検出がキー名集合ハッシュだけ | fix632: 中身ハッシュ台帳を持ち、変化時に `v292Dfix399_imgHash` を dirty 化 | 直接修正（既存ファイル無改造） |
| B | 版差分の対象が「生localStorageのv292av2_」=常に0件 | fix633: IDB実在庫を対象にした版差分スイープを**併走**させる | 直接修正（fix523のプリミティブを再利用） |
| C | `v292Dfix399_imgKeys`（自己修復の基準）が空 | fix631: 実在庫でシード（単調・縮まない） | 直接修正 |
| D' | `chrAiAv4:`（AI外見説明文）が同期対象外 | fix634: 束ねて既存 putimg チャネルで輸送 | 新規チャネル（Worker無改造） |
| 副 | ↻再生成でレシピ`.p`を`buildPrompt412()`が無条件上書き | **修正しない。観測口だけ出す**（fix631.promptDiff） | 観測のみ（§7） |

---

## 1. 監査（2026-07-26）の再検証結果 — 行番号のズレと**1件の事実誤り**

現コードを実際に読んで照合した。**確度: 確定（実ファイルを読んで確認）**

| 監査の記述 | 実物 | 判定 |
|---|---|---|
| 原因A: `v292Dfix399-cloudsync.js:361` 付近 `v292Dfix399_imgHash` | 実際は **61行(読み)/619行(判定)/734・1000行(書き)**。361行は無関係(`knownNs`周辺) | **行番号は誤り。中身は正しい**。619行 `var curHash = hash(imgKeys.slice().sort().join('|'))` がキー名集合ハッシュそのもの |
| 原因B: `localAvKeys()`/`localAv()` が生localStorageを見る（`fix523:159-165 / 65`） | `localAvKeys()`(159-162)は **`localStorage.length`/`.key(i)` を使う＝fix346がラップしていない＝確かに常に0件**。<br>**`localAv()`(65) は `localStorage.getItem` を使うので fix346 のラッパ経由で mem(IDB由来) を返す＝壊れていない** | **半分は誤り**。壊れているのは **`localAvKeys()` だけ**。<br>根拠: index.html の読み込み順は fix346(2932行) → fix523(3028行)。後から読まれた fix523 の `W.localStorage.getItem` は fix346 のラッパに解決される |
| 原因C: `v292Dfix399_imgKeys` が実測0件 | `saveExpectedImgKeys()` は **`idbWriteAll()` からしか呼ばれない**(216・224行)。`idbWriteAll` は `applySave`(=クラウド取り込み)と `selfHeal` のみ。**一度も取り込みをしていない端末では永久に空** | **正しい** |
| 原因D': `chrAiAv4:` が同期対象外 | `isGlobalKey()`(65-69行)は `v292avrec_` / `v292appr_` / `chr6_slots_meta` / `chr6_active_slot` / `chr6_epoch` / `genderMap_default` のみ。`chrAiAv4:` は無い → `collectLS` が拾わない | **正しい** |
| 副症状: `fix197:298-299` でレシピ`.p`を`buildPrompt412()`が無条件上書き | 298行でレシピ`.p`採用 → **299行で `if (p412) body.prompt = p412;` が無条件上書き** | **正しい（行番号も一致）** |

### 1.1 監査に無かった追加の発見（**要検証→確定**）

1. **fix519(icon-p2p)がまだ index.html に残っている**（3027行）。fix523のヘッダには「index.html では fix519 を外し fix523 のみ読む」と書いてあるが、**外れていない**。ただし fix519 は `v292Dfix519OnV1==='1'` の opt-in で**既定OFF**なので実害は無い。→ 今回は触らない（§8）。
2. **画像のサーバー側正本が2系統に割れている**。`op:put`(セーブ本体blobの `pkg.idb`) と `op:putimg`(D1 `images` テーブル) は**別の場所**で、`GET /img` は D1 images を優先して読む。fix523/fix402 は putimg 側だけを更新するので、**原因Aを放置すると blob 側の `pkg.idb` が古いまま固定される**。他端末が `op:get`→`applySave` すると `idbWriteAll(pkg.idb)` が**古い画像でローカルIDBを上書きする**。
   → これが「同期したら絵が戻った」の未報告の経路。**原因Aを直す最大の理由はここ**（単に「送られない」だけではない）。確度: 高確度（コード経路は確定、実機再現は未実施）。

---

## 2. 制約の確認（設計がどこに縛られているか）

- **C4（idempotencyキーにpayloadハッシュを使わない）** … fix634 は `mid` を**送らない**（Worker側 `if (mid && d1)` で idem予約自体が走らない）。競合検出は `baseImageRev`(CAS) に一本化する。
- **packageHash契約** … `chronicle-light-v1` のパッケージに**フィールドを足さない**。`isGlobalKey` に `chrAiAv4:` を足す案は採らない（理由は§5.1）。
- **localStorageに大容量を書かない** … fix632の中身ハッシュ台帳は約5KB、fix634の世代台帳は約8KB。画像・説明文の実体はLSに増やさない（`chrAiAv4:` は**既に**LSにある202件44KBで、増分ゼロ）。
- **削除処理を追加しない** … fix631の期待集合は**単調増加のみ**（縮めない）。fix634のマージは**union**のみ（サーバに無いキーをローカルから消さない／その逆もしない）。
- **fix517/fix516を壊さない** … §6。

---

## 3. fix631 — アイコン在庫の正本参照 ＋ 期待集合のシード ＋ 観測口

**ファイル**: `v292Dfix631-avatar-inventory.js` / ガード `window.__v292Dfix631` / OFF `v292Dfix631Off='1'`

### 3.1 何を直すか
- **原因B の土台**: 「この端末が持っているアイコンのキー一覧」を返す唯一の口 `window.__v292av.keys()` を作る。
  実体は **IndexedDB `chr6av`/`imgs` の `openKeyCursor`（値を読まない＝軽い）** ∪ **生localStorageの `v292av2_*`（fix346 OFF時の保険）**。
- **原因C**: 起動時に `v292Dfix399_imgKeys` を `union(既存, 実在庫)` で更新する。
  - **なぜ union か**: この値は fix399 の自己修復(`selfHeal`)が「足りないものを見つける」ための**期待集合**。実在庫より小さいと不足を検出できない（＝現状）。逆に**縮める**と「消えたことにする」ことになり、削除処理の新設に当たる（制約6違反）。よって**増やす方向にしか動かさない**。
  - 副作用: 正当に消したキャラのキーも期待集合に残り続ける。ただし `selfHeal` は既定OFF（`v292Dfix399SelfHealOn!=='1'`）で、かつ補充元にそのキーが無ければ何もしない。**実害なし**と判断。中央GC化が進んだ時点で期待集合の縮小規則をそちらへ寄せる。

### 3.2 API
```
window.__v292av = {
  keys(),            // string[]  … PREFIX無しのpk配列（キャッシュ・同期）
  refresh(cb),       // IDBから読み直す（起動時・60秒毎・画像書込時に自動）
  get(pk),           // dataURL or ''（localStorage.getItem経由＝fix346のmem）
  note(pk),          // 新しく書かれたpkをキャッシュへ即時追加（fix632から呼ぶ）
  status()
}
window.__v292Dfix631 = { __armed, on, keys, refresh, get, note, seedExpected, promptDiff, status }
```

### 3.3 観測口 `promptDiff(name)`（副症状の材料・書き込み無し）
`v292avrec_<pk>.p`（レシピ）と `__v292Dfix197.buildPrompt412(name)`（現在の設定から組み直した文）を並べて返す。
**判断材料を出すだけで、生成挙動は1バイトも変えない**（§7）。

---

## 4. fix632 — 原因A: 画像の「中身」の変化を検出する

**ファイル**: `v292Dfix632-img-content-dirty.js` / ガード `window.__v292Dfix632` / OFF `v292Dfix632Off='1'`

### 4.1 方式（fix399を1バイトも変えない）
fix399 は `curHash !== imgHashStored()` のときだけ full 送信する。
→ **`v292Dfix399_imgHash` に、キー集合ハッシュとしては絶対に生成されない値を書けば full 送信が起きる。**

- fix399 の `hash()` は `String(h>>>0)` ＝ **10進数字のみ**。
- よって sentinel を **`dirty632:<ts>`** とする。衝突不能。
- full 送信が成功すると fix399 自身が 734行で `v292Dfix399_imgHash = curHash`(キー集合ハッシュ) に戻す。**dirtyは自動で解除される**。失敗すれば dirty のまま残り、次回また full を試みる（望ましい）。

### 4.2 中身ハッシュ台帳
`localStorage['v292Dfix632_ih'] = { pk: "<len>:<djb2>" }`（fix523/Workerと同一の `hashFull` 契約）。約5KB。

- **書込検知**: `localStorage.setItem` を `v292av2_*` かつ `data:image` の時だけラップ。旧値と比較し、**中身が変わったときだけ**台帳更新＋dirty化。
  - ラッパの位置: fix632 は index.html の最後に読むので **fix632 → fix523 → fix346** の順に通る。旧値の取得は `getItem`（fix346のmem）から。
  - 自分が書く台帳・sentinel は**捕獲した元の setItem** で書く（再入防止）。
- **起動時の照合**（IDB直書きなど setItem を経ない変化の受け皿）: fix631 の在庫を舐めて台帳を再計算し、差があれば1回だけ dirty 化。
- **初回シード（一度きり）**: 台帳が無い端末では「今の絵がサーバへ上がっているか」を知る手段が無い。
  `v292Dfix632_seeded` が未設定なら、台帳を作ったうえで **1回だけ dirty 化**して full 送信を1回起こす。
  → 既存の乖離（過去に再生成したのに blob 側が古いまま）をこの1回で解消する。コスト: 約5MBの送信1回。**意図的**。

### 4.3 コスト
再生成のたびに fix399 の push が light(約65KB) から full(約5MB) に上がる。
新規キャラ生成は**元々**キー集合が変わるので full だった。増えるのは「既存キャラの↻再生成」の分だけ。頻度は低く、ユーザー起点なので許容する。
（もし実機で重ければ `v292Dfix632Off='1'` で即座に旧挙動へ戻る。）

---

## 5. fix633 — 原因B: 全在庫のアイコン版差分スイープ

**ファイル**: `v292Dfix633-icon-sweep-full.js` / ガード `window.__v292Dfix633` / OFF `v292Dfix633Off='1'` / shadow `v292Dfix633Shadow='1'`

### 5.1 なぜ fix523 を書き換えず「併走」にするか
`localAvKeys()` は fix523 のクロージャ内部関数で、`recvSweep` から直接呼ばれている。外から差し替えられない。
一方 fix523 は **`pullOne` / `pushOne` / `revGet` / `revSet` / `hashFull` / `on` を `window.__v292Dfix523` に公開している**。
→ **判断（どのキーをどうするか）だけを fix633 が作り直し、実行は fix523 のプリミティブに委譲する**のが最小変更。
409連発の打ち切りカウンタ(`conflictN`)も fix523 の中で共有されるので、二重に暴走しない。

### 5.2 スイープ規則（fix523 と完全に同一の判定を、対象集合だけ正しくして再実装）
```
manifest = op:imgmanifest（10秒TTLでキャッシュ・fix634と共用）
対象 = __v292av.keys() ∪ {manifestにあってローカルに無く、かつ画面に映っているpk}
除外 = v292Dfix402_pimg に載っている送信待ちキー
各キーについて:
  server無し                       → 何もしない
  local無し                        → PULL
  server.hash === local.hash       → rev だけ採用（通信しない）
  server.rev > 既知rev             → PULL（サーバーが厳密に新しい）
  それ以外                         → PUSH（baseImageRev付き＝409ならfix523がPULLへ切替）
```
- iOS配慮: 1スイープ最大6キー・round-robin・キー間120ms。周期25秒（fix523の20秒と位相をずらす）。初回9秒。
- **fix523がOFFのときは fix633 も動かない**（`__v292Dfix523.on()` を見る）。緊急停止スイッチを1本に保つ。
- **shadow モード**: `v292Dfix633Shadow='1'` で判定だけ行いログに残す。PULL/PUSHは撃たない。実機で「何件をどう判定したか」を先に見たいとき用。

### 5.3 fix523 との二重実行
fix523 の対象集合は実質 `{画面に映っていてローカルに無いキー}` だけ（`localAvKeys()`が空のため）。fix633 の対象と重なるのはその一部。
重なっても `pullOne` は冪等（同じ dataURL なら書かない）、`pushOne` は CAS 付き。**害は無い**。位相をずらして同時発火を減らす。

---

## 6. fix634 — 原因D': `chrAiAv4:` の同期（輸送設計）

**ファイル**: `v292Dfix634-aiappearance-sync.js` / ガード `window.__v292Dfix634` / OFF `v292Dfix634Off='1'`

### 6.1 なぜ `isGlobalKey` に足さないのか（却下した案）
`isGlobalKey()` に `/^chrAiAv4:/` を足せば1行で同期対象になる。**しかし採らない**:
1. `pkg.ls` は**毎ターンの light push に載る**。現在約65KB＋44KB＝**約1.7倍**。モバイル回線とD1書込に毎ターン効く。
2. `pkg.ls` の中身が変われば `packageHash` が変わる。fix590/fix596 の三者照合の入力を、アイコン都合で毎ターン揺らすことになる。**セーブ同期の合意形成にアイコンの都合を持ち込まない**。
3. `collectLS` は `chr6*` のスロット分離・墓標フィルタの対象。外見説明文は**スロット横断のグローバル資産**で、性質が違う。

### 6.2 採用案 — 既存 putimg チャネルの「別キー枠」に**束ねて1件**で置く

Worker の `op:putimg` は `k`(≤128文字) と `data`(≤2MB) を受ける**汎用のkey-value**で、画像であることを要求していない。
`GET /img` だけが `data:<mime>;base64,` 形式を要求する（`handleImg` 1633行）。
→ **`data:application/json;base64,<...>` として置けば、Worker無改造で読み書きできる。**

```
サーバーキー : v292meta1_aiav        （1件だけ。v292av2_ で始まらないので fix523/fix633 は無視する）
値           : data:application/json;base64,<base64(JSON)>
JSON         : { v:1, at:<ms>, m: { "<localStorageのキー>": { v:"<外見説明文>", t:<ms> } } }
競合制御     : op:putimg の baseImageRev（画像と同じCAS）。409 → 取り直してマージ → 1回だけ再送
受信         : op:imgmanifest の rev/hash（fix633 と共用の1回の取得）→ 差があるときだけ GET /img
idempotency  : mid を送らない（制約C4）
```

- **なぜ1件に束ねるか**: キー別に置くと初回同期で **202リクエスト**（1件6秒間隔なら20分）。束ねれば **1リクエスト**。
  実測44KB → JSON約60KB → base64約80KB。上限2MBに対して十分小さい。
- **キー長の問題を回避**: `chrAiAv4:<日本語名>::<hash>` は128文字に収まらない可能性がある。束ねると**サーバーキーは固定1本**になり、実キーは payload の中に入るので制限を受けない。

### 6.3 マージ規則（収束すること・絵が振動しないこと）
```
両方にある : t が大きい方を採用。t が同値なら「文字列として小さい方」を採用（決定的なタイブレーク）
片方だけ   : そのまま採用（union。消さない）
```
- ローカルの世代 `t` は `localStorage['v292Dfix634_t'] = { lsKey: t }`（約8KB）で持つ。
- **既存の（この機能より前からある）エントリは t=0** として扱う。→ サーバー側に既に登録があればそちらが勝つ＝**全端末が最初に登録された1つの説明文へ収束する**。これが「別端末で別の絵になる」の直接の解。
- **ユーザーが↻で作り直したときだけ**、`setItem` ラッパが `t=Date.now()` を記録し**ローカルが勝つ**→push。意図した作り直しは伝播する。
- 時計ずれ: 端末間の時計ずれ分だけ「どちらが新しいか」を誤り得る。ただし**誤っても収束はする**（どちらか一方に決まる）。振動しない。確度: 高確度。

### 6.4 安全弁
- ローカルへ書き戻すのは **`chrAiAv4:` で始まるキーだけ**（payload に別のキーが混ざっていても書かない）。
- 1値2000文字・全体1.5MB・800エントリで上限を切る（超過分は t の古い順に落とす。**ローカルのLSからは消さない**、束から外すだけ）。
- 書き戻しは try/catch で QuotaExceeded を捕まえ、1件でも失敗したらその回のマージを中断してログに残す。
- 送信は 8秒デバウンス＋「束のhashが前回送信時と同じなら送らない」。

### 6.5 Worker の差分
**なし。** `worker/` 配下および `chronicle-proxy-v26_ns.js` は1バイトも変更していない。

---

## 7. 副症状（↻でレシピと違う絵になる）— **意図的に修正しない**

`v292Dfix197-avatar-key.js:298-299`
```js
try { var rec403 = ...; if (rec403 && rec403.p) { body.prompt = rec403.p; ... } } catch(e){}
try { var p412 = buildPrompt412(info.name||''); if (p412) body.prompt = p412; } catch(e){}  // ← 無条件上書き
```
これは fix412 の**意図した設計**（「固定レシピ文の使い回しをやめ、現在のキャラ設定＋世界観から組み直す」）。
つまり「レシピを無視する」のはバグではなく**決定**であり、覆すのは**見た目の方針変更＝おしんの決定事項**。実装担当が勝手に戻してよい種類の変更ではない。

→ 本パッチでは **観測口だけ** 出す（fix631）。
```js
__v292Dfix631.promptDiff('白石澪')
// → { name, pk, hasRecipe, same:false, recipeP:'...', p412:'...', lenR, lenP }
```
おしんが実物を見て「レシピを優先したい」と決めたら、`v292Dfix412Off='1'`（**既存のスイッチ**）で今すぐ従来挙動へ戻せる。新しいコードは要らない。

---

## 8. 意図的にやらないこと

| 項目 | 理由 |
|---|---|
| fix519(icon-p2p) の index.html からの撤去 | 既定OFFで無害。読み込み順の変更はリスクだけが残る |
| `v292Dfix399_imgKeys` を実在庫まで**縮める** | 削除処理の新設に当たる（制約6）。中央GC化の担当範囲 |
| `isGlobalKey` への `chrAiAv4:` 追加 | §6.1。packageHash契約と毎ターン転送量 |
| Worker(v26)の変更 | 既存3opで足りる。デプロイ面を増やさない |
| BUILT / version.txt の更新 | **デプロイ担当（おしん）の作業**。ここで動かすと本番と食い違う。`index.html` を配信する回に必ず両方を同値へ |
| fix197 の生成プロンプト方針の変更 | §7 |

---

## 9. リスクとロールバック

| リスク | 影響 | 戻し方 |
|---|---|---|
| fix632 の初回シードで5MBのfull pushが1回走る | モバイルで数秒〜十数秒。fix399側は既存の経路なのでデータ破壊は無い | `v292Dfix632Off='1'`（シード前なら発生しない。発生後は台帳が残るので二度目は無い） |
| fix632 により再生成のたびに full push | 通信量増 | `v292Dfix632Off='1'` → 即座に旧挙動（キー集合ハッシュのみ） |
| fix633 のスイープでリクエストが増える | imgmanifest 25秒毎＋最大6件のGET/POST | `v292Dfix633Off='1'`。事前確認は `v292Dfix633Shadow='1'` |
| fix633 と fix523 の二重PULL | 無害（冪等）だが通信が重複 | 同上 |
| fix634 が誤ったキーをLSへ書く | `chrAiAv4:` 以外は書かない実装＋テストで固定 | `v292Dfix634Off='1'` |
| fix634 の80KB pushが失敗し続ける | 説明文が伝播しないだけ。既存機能は無影響 | 同上 |
| 全部まとめて止めたい | — | `v292Dfix631Off=v292Dfix632Off=v292Dfix633Off=v292Dfix634Off='1'` → 完全に fix630 時点の挙動 |
| さらに元から止めたい | — | index.html の末尾4行の `<script>` を削除（他の読み込み順に影響しない位置に置いてある） |

**セーブデータ(`chr6*`)のスキーマは1バイトも変えていない。削除処理も追加していない。**

---

## 10. 検証（node）

`test_fix631.cjs` / `test_fix632.cjs` / `test_fix633.cjs` / `test_fix634.cjs` を追加。
実チェーン方式（`vm.runInContext` ＋ モック `window`/`localStorage`/`indexedDB`/`fetch`）。

**fix517/fix516 の回帰固定**（制約3）は `test_fix632.cjs` に含める:
- fix517: ローカルに実画像がある → `fix400.urlFor` が `''` を返す／無い → 元URLを返す（fail-open）。**fix632のsetItemラッパを噛ませた状態で**同じ結果になること。
- fix516: opt-in（`v292Dfix516OnV1==='1'`）でのみ armed。凍結pk一致かつローカル有のときだけ `''`。ローカル無なら元URL。
- fix632 の dirty sentinel が fix399 の `hash()` 出力（10進数字列）と**絶対に衝突しない**こと。
