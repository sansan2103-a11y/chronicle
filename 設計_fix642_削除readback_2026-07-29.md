# 設計 fix642 — 物語削除の「read-back 次段」 (2026-07-29)

対象: `v292Dfix642-delete-readback.js`（home.html にのみ搭載・操作専用・自動実行なし）
前段: fix587（削除の正規サービス）/ fix588 / fix602（墓標バリア）/ fix564（復元セット）/ fix569（DeleteGateway）

---

## 0. なぜこれを作るか（前段が残した宿題）

`v292Dfix587-story-lifecycle.js` の `continueExisting()`（:441〜455）に、この段の宿題が明記されている。

> 照合は通った。だが**クラウドで墓標が確定しているかを推測してはいけない**。
> ★read-back の口がまだ無い（fix399x に「remote meta だけ読む」APIが無い）ので、
> この異常系では**物理削除しない**。
> → read-back API を作った段（次段）で、ここを「新しい planId を発行して削除」へ格上げする。

いま実機に残っているのは 2 件（どちらも `blocked-stale-legacy`）。

| slotId | 状態 | 実体 |
|---|---|---|
| `smrnoszes2j` | 港の倉庫街・6T | 削除計画作成後に home が本体へ2ターン書き足した（12785B → 18132B）。exact一致関門が fail-closed で停止＝**正しい動作** |
| `sms37m8r0ae` | 0ターン | 掃除対象。同じく stale |

停止の実体は `v292Dfix587_refusals` の `code:'stale'`（expectedBytes ≠ actualBytes）。
墓標はローカル `chr6_slots_meta` に `deleted:true, deleteOpId` で立っている。
**クラウド側の墓標が確定しているかは推測してはいけない** — だから read-back が要る。

---

## 1. read-back に選んだ op と理由

### 採用: `op:'commitstate'` + `slotId`（Worker v26・既存op・Worker変更なし）

`chronicle-proxy-v26_ns.js` :1703〜1737 を実読して確認した事実。

```js
if (op === 'commitstate') {
  if (!d1) return json({ ok:false, errorCode:'unsupported' }, 501);   // 嘘の0を返さない
  const row = await env.DB.prepare('SELECT rev, blob, package_hash, ... WHERE u=?1 AND kind=?2')...
  if (!row) return json({ ok:true, ..., exists:false });
  const slotId = (body && body.slotId != null && body.slotId !== '') ? String(body.slotId).slice(0,64) : null;
  if (slotId) out.tombstone = tombstoneOfSlot(row.blob, slotId);
  return json(out, 200);
}
```

`tombstoneOfSlot(blob, slotId)`（:同ファイル）は canonical blob の `ls['chr6_slots_meta']` から
**そのslotIdのエントリだけ**を取り出し、`{ deleted, deleteOpId, lifecycleVersion, recoverySnapshotId }`
を返す。これはまさに「remote meta だけ読む API」であり、fix587 が無いと言っていた口は
**v25/v26 で既に生えていた**（fix587 の当時は未実装だった）。

選定理由:

1. **読取専用**。Worker コメントに「これは**読取専用**。1バイトも書かない」「ensureNs ではなく nsFor を使う」と明記。canonical に触れない。
2. **応答が墓標そのもの**。`deleted` / `deleteOpId` を直接照合できる。ローカルの `deleteOpId` と突き合わせて「この削除操作が確定しているか」を判定できる。
3. **軽い**。canonical blob 全体を返さない（`rev`/`packageHash`/`tombstone` だけ）。
4. **D1 が無い環境では 501 を返す**＝「使えない」と正直に答える。ここで嘘の `ok` を作らないので、fail-closed に素直に落とせる。

### 却下: `op:'get'`

- canonical pkg 全体（数百KB〜）を返す。iPhone Safari で無駄が大きい。
- ★何より**事故経路が近い**。`op:'get'` の応答は `applySave()` へ流す形が既存コードに存在する
  （fix399 `pullData()` / home の `pull()`）。fix642 がここを触ると「読むだけのつもりが復活・上書き」を
  作る危険がある。fix642 は `op:'get'` を**呼ばない**。

### 却下: `op:'meta'`

`{updatedAt, device, size, rev, ns}` だけで、墓標の中身を持たない。判定に使えない。

### 通信の作り方

home.html は fix399 を積んでいない（`test_fix589.cjs:200` がそれを固定している）。
そこで fix642 は home の `proxyUrl()` / `authHeaders()` と**同じ規則**で自前に組む（読取専用の1往復のみ）。

- URL: `localStorage['v292ProxyUrl']` → 無ければ `https://novel-proxy.sansan2103.workers.dev`
- ヘッダ: `x-chronicle-pass`（合言葉）/ `x-google-id`（`v292GoogleToken` の exp は**秒**。fix601 と同じ `exp*1000 > now+30000` 判定）
- タイムアウト 20秒（AbortController）

---

## 2. read-back の判定表（`readbackTombstone`）

| サーバ応答 | code | ok | 意味 |
|---|---|---|---|
| `tombstone.deleted===true` かつ `deleteOpId` 一致 | `confirmed` | **true** | クラウドの正本に、この削除操作の墓標が確定している |
| `tombstone.deleted===true` だが `deleteOpId` 不一致 | `remote-deleteopid-mismatch` | false | 別の削除操作の墓標。この計画の根拠にはできない |
| `tombstone` はあるが `deleted!==true` | `remote-alive` | false | **別端末で復活/復元されている**。絶対に消さない |
| `tombstone===null`（metaに該当idが無い） | `remote-tombstone-missing` | false | 削除が伝わっていない |
| `exists===false`（canonical行が無い） | `remote-empty` | false | クラウドに正本が無い。次のpushで復活しうる |
| HTTP 501 / `errorCode:'unsupported'` | `readback-unsupported` | false | D1無し。読み戻せない |
| HTTP≠200 / `ok!==true` / 例外 | `readback-failed` | false | 通信失敗 |
| 未ログイン | `not-logged-in` | false | ヘッダが作れない |

**`ok:true` は `confirmed` のときだけ。** それ以外は全部 fail-closed（＝消さない）。

---

## 3. `resumeBlocked(slotId, {confirmStale:true})` の手順

```
(0) OFF/枠の確認   v292Dfix642Off / slotId が 'chr6'|'default' → 拒否
(a) confirmStale   無ければ code:'need-confirm' で拒否（1バイトも書かない）
(b) 前提の確認     依存(fix562/564/569/587) / fix587がOFFでない /
                   v292Dfix587_blocked に該当あり / ローカル墓標 deleted:true /
                   deleteOpId あり / restoreOfDeleteOpId で復元済みでない
(c) read-back      readbackTombstone() → 未確定なら墓標を再push → 再read-back
                   それでも未確定 → code:'cloud-tombstone-unconfirmed' で停止
(d) ★復元セット    **必ず現在値で撮り直す**（後述）→ 検証 → 計画キーの被覆確認
(e) 新planId       現在のキー実体から exact スナップショット(keys+bytes+hash)で新planを発行
(f) 削除           DeleteGateway(fix569.tryDeleteExact) 経由のみ。自前 removeItem を呼ばない
(g) 履歴           旧blockedレコードは消さず superseded:<新planId> を付ける
(h) 返却           消したキー数・拒否の内訳・使った復元セットIDを返す
```

### (c) 再push について

home.html には fix399 が無いので、fix642 の `pushTombstone()` は
`window.__v292Dfix399x.push()` が居るときだけ動く。居なければ
`code:'push-unavailable'` を返し、**自前で forceput を組み立てることは絶対にしない**
（正本の上書きを patch モジュールが持つのは削除事故の作り方そのもの）。
home で未確定だった場合、人がやる行動は「☁ いま上げる」（＝墓標入りの meta を canonical へ送る。
`test_fix602_home` の S3 が「meta の墓標そのものは送る」を固定している）か、ゲーム画面を一度開く。
それを `hint` に日本語で返す。

### (d) なぜ「旧スナップショットが実在するか」だけでは不足か

指示は「旧計画の `recoverySnapshotId` が実在するか確認し、**実在しない場合は**新しい復元スナップショットを撮る」だが、
fix602 のコメントが挙げた反例 C がここで効く。

> C: 今回の計画は11件中6件が既に消えている。**現在値だけ**をスナップショットすると、
>    既に消えた6件の復元元が失われる。旧スナップショットと現在値を統合した
>    「後継の完全な復元セット」でなければならない。

いま止まっている 2 件は **stale**（12785B → 18132B）＝ 旧スナップショットの中身は
**これから消すバイト列と違う**。旧スナップショットが「実在して検証も通る」場合ですら、
それだけを根拠に現在値を消すと**追記された2ターンが戻せない**。

→ fix642 は **常に現在値で新しい復元セットを撮る**（上位互換。指示の条件を包含する）。
　 旧スナップショットは**消さない**（`priorRecoverySnapshotId` として計画に記録し、LSにも残す）。
　 結果、後継の復元セットは「旧スナップショット（削除時点）＋新スナップショット（現在値）」の
　 2 本立てになり、反例 C の「既に消えた分の復元元が失われる」も起きない。

作成は fix564 の論理スナップショットAPI（`create(slot,{now,reason})` → `verify(id)`）をそのまま使う。
`reason` は **`'lifecycle-delete:' + deleteOpId`** に揃える。fix587 の `checkResumable()` が
`man.reason === 'lifecycle-delete:'+deleteOpId` を要求するので、この形以外にすると
fix587 側の再開判定が将来落ちる。deleteOpId は**作り直さない**（クラウドの墓標と食い違うため）。

**被覆確認（★これが「戻り道」の証明）**: 新計画の全キーについて
`manifest.parts[key]` が存在し `bytes` と `hash` が現在値と一致することを確かめる。
1つでも欠ければ `code:'snapshot-incomplete'` で停止。

**フォールバック**（`chr6_slot_<slot>` が既に無く fix564.create が本体不在で断る場合）:
fix564 と**同じレイアウト**（manifest `chr6_snap_<slot>_<ts>` / 実体 `chr6_snapd_<slot>_<ts>_<n>`、
`complete:true`、各パート read-back 照合）で自前に書く。新形式は作らない。
これにより `fix564.verify()` / `restore()` / `remove()` がそのまま使える。
途中で失敗したときの後始末も **DeleteGateway 経由**で行う（自前 removeItem を呼ばない）。
`chr6_snap_del_<slotId>_<ts>` という独自形式は**採らない** — `chr6_snap_` は fix564 の manifest 名前空間で、
`fix564.list()` が壊れた manifest として拾ってしまうため。

### (e) 新 plan の形式（fix587 の plan と同一形式・追加フィールドのみ）

```js
{
  planId: 'plan_' + deleteOpId + '_r' + ts,   // ★新planId。deleteOpId は変えない
  deleteOpId: '<既存のまま>',
  slotId: '<slotId>',
  snapshotId: 'chr6_snap_<slot>_<ts>',        // ★今回撮り直した復元セット
  createdAt: ts,
  lifecycleVersion: 1,
  keys: [{ key, bytes, hash }, ...],          // ★いまの実体から。hash は fix562._hash(FNV-1a)
  source: 'fix642:resume',
  /* --- fix642 の追加分（fix587 の読み手は無視できる） --- */
  supersedesPlanId: '<旧planId>',
  priorRecoverySnapshotId: '<旧 recoverySnapshotId>|null',
  priorSnapshotVerified: true|false
}
```

`keys` の作り方は fix587 の `planKeys()` と同じ（本体 `chr6_slot_<slot>` ＋ `fix562.sideStoreKeys(slot)`、
値が null のキーは載せない）。hash は fix562 の `_hash`（fix569 の `fnv` と同一実装・同一書式）。

### (f) 削除実行

```js
window.__v292Dfix569.tryDeleteExact({
  key, expectedBytes, expectedHash,
  intent: 'lifecycle-delete', path: 'fix642',
  deleteOpId: deleteOpId,                       // fix570/588 の deleteOpId 要求
  reason: 'story-delete-resume plan=' + planId
})
```

- fix569 は **fix246 より前に捕捉した native primitive** で exact key を消し、read-back で消えたことを確認する。
- fix642 は `localStorage.removeItem` を**一度も書かない**（`scan_delete_api.cjs` の direct/bypass パターンに1件も当たらない）。
- 既に存在しないキーは fix594 と同じく成功扱い（`alreadyMissing` として別に数える）。

---

## 4. 失敗系の一覧（全部 fail-closed ＝ データを消さない）

| code | 起きる場面 | 消すか |
|---|---|---|
| `service-off` | `v292Dfix642Off='1'` | 消さない |
| `need-confirm` | `confirmStale:true` が無い | 消さない（1バイトも書かない） |
| `not-deletable` | slotId が `chr6` / `default` / 空 | 消さない |
| `missing-deps` | fix562/564/569/587 のどれかが無い | 消さない |
| `lifecycle-off` | `v292Dfix587Off='1'` | 消さない |
| `not-blocked` | `v292Dfix587_blocked` に該当が無い（対象外の物語） | 消さない |
| `no-tombstone` | ローカル meta に `deleted:true` が無い | 消さない |
| `no-delete-op-id` | 墓標に deleteOpId が無い（malformed） | 消さない |
| `restored` | `restoreOfDeleteOpId` がこの deleteOpId と一致＝正式に復元済み | 消さない |
| `already-deleted` | 計画対象キーが1つも残っていない | 何もしない（成功扱い） |
| `cloud-tombstone-unconfirmed` | read-back（＋再push＋再read-back）で確定できない | **消さない** |
| `snapshot-failed` | fix564.create もフォールバックも失敗（容量不足等） | 消さない |
| `snapshot-unverified` | 復元セットの hash 照合に落ちた | 消さない |
| `snapshot-incomplete` | 計画キーが復元セットに1つでも含まれない | 消さない |
| `partial` | ゲートが1件以上拒否（protected 等） | 消せた分だけ。旧blockedは superseded にしない |
| `deleted` | 全キー削除完了 | 完了。旧blockedに superseded を付ける |

**自動実行しない**: boot フック無し・タイマー無し・`window` へ関数を出すだけ。
`readbackTombstone` 以外は通信しない。`readbackTombstone` も呼ばれたときだけ1往復する。

---

## 5. 配線と、既存テストへの影響

- `home.html` に `<script src="v292Dfix642-delete-readback.js?cb=v292Dfix641"></script>` を **fix602 の後**に追加。
  index.html には**入れない**（このモジュールは操作専用で、ゲーム画面に居る必要が無い）。
- ★cb は `?cb=fix642` ではなく **`?cb=v292Dfix641`**（＝現在の HOME_BUILT `20260729-fix641`）にした。
  `test_fix602_home` (8) が「home の主要モジュールの cb == HOME_BUILT」を機械検査しており、
  形式も `\?cb=v292Dfix(\d+)` で固定されているため。fix642 をその検査対象**に加えた**ので、
  出荷時に HOME_BUILT を上げるときは fix642 の cb も一緒に上がっていないとテストが落ちる。
- `test_fix602_home` (8) の「位置は fix587 の直後」判定を修正した。
  旧: `!/v292Dfix587-story-lifecycle\.js[\s\S]*?<script src="v292Dfix(?!602)/.test(HOME)`
  → fix587 の**後ろにある 602 以外のあらゆる script** に反応するので、fix642 を足すと必ず落ちる。
  新: 「fix587 と fix602 の**間**に別の `<script src="v292Dfix` が無い」で見る。
  固定したい契約（fix587 の直後に fix602 が来る）は変えていない。

---

### 5b. fix587 への1行変更（片づいた後に嘘の警告を残さない）

`v292Dfix587-story-lifecycle.js` の `pendingSummary()` だけ、`superseded` が付いた
blocked レコードを**表示から外す**（1行）。

```js
var p = readPending(), b = readBlocked().filter(function(x){ return x && !x.superseded; });
```

理由: fix642 は指示どおり旧レコードを**消さずに** `superseded` を付けて履歴に残す。
除かないと、片づけが完了した後もホーム画面が「削除の後片づけを停止しました」と言い続ける
＝**嘘の警告**になる。`blockedDeletes()`（履歴の口）は従来どおり全件を返す。
★このファイルの中身を変えたので、出荷時は **index.html と home.html の両方**で
fix587 の `?cb=` を上げる必要がある。

---

## 6. `test_fix642.cjs` が固定する契約

1. `confirmStale` 無しは拒否（`need-confirm`、LSは1バイトも変わらない）
2. クラウド墓標が未確定（missing / alive / mismatch / empty / unsupported / 通信失敗）なら**消さない**
3. 新 planId が**現在のバイト**の exact（keys+bytes+hash）で作られる（旧計画の古い bytes を使わない）
4. 削除は **DeleteGateway 経由**（モックで捕捉。`intent:'lifecycle-delete'` / `path:'fix642'` / `deleteOpId` つき）
5. 復元スナップショットが作れない・検証に落ちる・計画キーを被覆しないなら**消さない**
6. `chr6` / `default` は拒否
7. `v292Dfix642Off='1'` で全 API が拒否（通信もしない）
8. 旧 blocked レコードは消えず `superseded` が付く
9. read-back は `op:'commitstate'` + slotId を使い、**`op:'get'` を呼ばない**
