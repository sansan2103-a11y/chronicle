# 設計: fix643（崩壊ターンの救済生成）/ fix644（fix640 採取の厳格化）

2026-07-29 / ブランチ v292-rebuild / BUILT は据え置き（`20260729-fix642`）
デプロイ・push はしていない。

---

## 0. 結論（先に）

| | fix643 | fix644 |
|---|---|---|
| 何をした | 崩壊判定を **保存の前** へ移し、hard(7点以上) なら1回だけ書き直させる層を新設 | fix640 の採取に「人物として使われた構文」条件を入れ、ゴミ候補を止めた |
| 既定 | **shadow**（判定と記録だけ・挙動は変えない）。実弾は `v292Dfix643Live='1'` の端末のみ | 常時（採取の入口の条件なので段階導入の対象ではない） |
| 新ファイル | `v292Dfix643-collapse-rescue.js` | なし（fix640 を直接修正・前日出荷の新モジュールのため） |
| テスト | `test_fix643.cjs`（105件・新規） | `test_fix640.cjs` に追加（87→148件） |
| 確度 | 配線位置＝**確定**（index.html のソースで固定）／実効果＝**要検証**（実機 shadow の実測待ち） | 実例4件の除外・実例5件の採取＝**確定**（テストで固定） |

全テスト: **66ファイル / 3980件 / 失敗0**（実測）。前回基準は 65ファイル 3814件。

---

## 1. fix643 — 配線位置とその根拠（★ここが本題）

### 1-1. 実際の順序を読んだ結果

`index.html` の `G.submit`（2026-07-29 現在の行番号）:

```
1805  result = await Api.call(sys, user)            ← 本編の生成
1809  if (!result) { 入力を戻して return }           ← fix263a2 の既存ガード
1808  plan = Planner.parsePlan(result.text, S.mode)
1828  （fix216/fix235）再掲・短文なら Api.call(sys, user + '…') で1回書き直し
1861  PsychEngine.process(plan)                     ← 状態更新の起点
1863- 会話ログ生成 / <say>整形 / fix213-277 の後処理
1988  const turn = {...}
1993  S.turns.push(turn)
1994  S.scene.branches = ...
1995  S.save()                                      ← クラウドpushはここから後ろ
1996  UI.appendTurn(turn, S.turns.length - 1)
```

**turn が S.turns へ入り S.save が走る前**に介入できる同期的な分岐点は、実質
`Api.call` の戻り値 **1箇所しかない**。`parsePlan` は同期関数なので救済生成（非同期）を挟めず、
`UI.appendTurn` はすでに push と save が済んでいる。

### 1-2. なぜ `Api.call` ラップなのか（4つの根拠）

1. **fix555 が同じ場所を使っている実績**
   `v292Dfix555-punct-repair.js` の冒頭に「入る場所 = Api.call の直後・Planner.parsePlan の**前**」と
   明記されている。句読点崩れの1回修復がこの位置で動いており、観測点として実績がある。
2. **ターン不成立が「既存の経路」で実現できる**
   1809 の `if (!result) { … inputEl.value = text … return; }` は fix263a2 が入れたガード。
   `Api.call` が **falsy を返すだけ**で、turn の組み立て・`S.turns.push`・`S.save`・`UI.appendTurn` が
   まとめてスキップされる。落とし穴①〜④は個別の細工ではなく、この1つの事実から自動的に従う。
3. **sys 注入経路を壊さない**
   救済の追加指示は `user` の末尾へ足す。これは 1828 の fix216/fix235 が既にやっている作法と同じで、
   `Planner._extensions` にも fix482 の sys 署名にも触れない。
4. **features.js を触らない**
   ラップだけで入る。`features.js` に `v292Dfix643` の文字列は1つも無い（テストで固定）。

### 1-3. どの `Api.call` が本編かの見分け方

`index.html` 内の `Api.call(` は5箇所（テストで件数を固定）。

| 場所 | 引数 | 種別 |
|---|---|---|
| 1725 `genConvLog` | `(sys2, user2, 500, {…})` | 補助 |
| 1749 `attribQuotes218` | `(sys2, user2, 400, {…})` | 補助 |
| 1805 本編 ×2（初回＋429/5xx の透過リトライ） | `(sys, user)` | **本編** |
| 1828 fix216/235 の書き直し | `(sys, user + '…')` | **本編** |
| features.js 988 / 1826 | `(pr.sys, pr.user, 2400 / 1000)` | 補助 |

→ **maxTok を渡さない呼び出しだけが本編**。
sys の文言で見分ける方式（fix482 の `SYS_SIG`）は採らなかった。新エンジン(fix417c)では
`sys` が空文字になる経路があり、いま index.html/features.js に `【出力の形式` の文字列は存在しない。

### 1-4. 判定は fix624/fix626 をそのまま呼ぶ

`window.__v292Dfix624.scoreTurn(...)` を呼ぶだけ。**新しい検出器は作っていない**（テストで固定）。
閾値も fix626 の裁定のまま `score>=7 = hard`。

ただし1点だけ、この層は fix624.sweep と**分母が違う**:

- fix624.sweep は「会話カードの激減」を `turn._convSays.length` で測る。
- fix643 が動く時点では `_convSays` はまだ存在しない（submit の後半で作られる）。
- → **いまの応答も過去のターンも `<say>` タグの数で測る**。
  過去は `turn.plan.narrative`（fix640 の実測で `<say>` が残っていることが確認済み）。
- 台詞タグを使わない物語では現在も過去も 0 になり、`cardAvg=0` ＝この副次シグナルが自動的に無効になる。
  片側だけ 0 になって毎ターン「カード0」が立つ事故は構造的に起きない。

この差のため、shadow の記録と `fix624.sweep()` の点数が 1〜2 点ずれることがある。**要検証**の項目。

### 1-5. 処置の流れ（live のときだけ）

```
1回目 hard
  → 救済生成 1回（user 末尾へ異常コードだけを足す。★壊れた本文は渡さない）
     直った → その本文を採用（outcome=regen-ok）
     まだ hard → Api.call は null を返す（outcome=regen-hard）
        = ターン不成立。入力は残る。ターン番号は増えない。保存もしない。
        + 案内バナー（操作3つ）
```

異常コードは fix624 の hits 名を写しただけ:

| fix624 hits | コード |
|---|---|
| 反復 | `repetition-loop` |
| 助詞が少ない | `register-collapse` |
| ルビ過多 | `ruby-abuse` |
| 自問自答 | `self-dialogue` |
| 人称混在 | `person-mix` |
| 長すぎる文 / 平均文長 | `over-long-sentence` / `long-sentences` |
| 読点が少ない | `punctuation-drop` |
| カード0 / カード激減 | `dialogue-missing` |

`dash-abuse` は写像表に席だけ用意してあるが、fix624 に対応する hit がまだ無いので**現状は出ない**
（新しい検出器を勝手に足さないため。fix627/628 が測っている自己反復を点数へ入れる段で足す）。

### 1-6. 2回目 hard のときの UX（GPT指定の3操作）

| ボタン | 何をするか |
|---|---|
| もう一度試す | 入力を戻して `G.submit()` を呼ぶ。**ユーザー起点の新規生成**であって自動の3回目ではない |
| 入力を直す | バナーを消して入力欄へフォーカスするだけ。生成しない |
| 最初の文章を確認する | 誤検出の逃げ道。**通信せずに**保持していた1回目の候補をそのまま採用し、`outcome=user-accepted` / `note=user-accepted-hard-candidate` で記録 |

「最初の文章を確認する」の実装:
hard 候補を **メモリだけ**に保持（`held`）→ ボタンで `accept` に移して `G.submit()` を呼ぶ →
`Api.call` ラッパが `accept` を見て**内側を呼ばずに**その候補を返す。
API を余分に叩かず、同じ本文が決定的に再現される。`accept` は submit の promise が決着したら必ず消す。

### 1-7. 落とし穴7つの担保

| # | 落とし穴 | 担保 | テスト |
|---|---|---|---|
| ① | エラー表示でターン数が増える | `Api.call` が null → 1809 で return（push より前） | (0)(3) |
| ② | 入力欄が空になる | 1809 が `inputEl.value = text` で戻す | (3) |
| ③ | 却下した試行で fix77/fix190/longmem が動く | `PsychEngine.process` は 1861＝ガードより後ろ。parsePlan すら走らない | (0)(3) |
| ④ | hard 候補がクラウドへ出る | `S.save()` が走らない＝差分が生まれない | (0)(3) |
| ⑤ | 連打で並列生成 | 自前 `busy` フラグ＋`S.inFlight`。ボタンは押した瞬間にバナーごと消える | (5) |
| ⑥ | ページ復帰で勝手に再生成 | 保留は**メモリだけ**。localStorage には何も残さない。起動時に生成する経路が無い | (6) |
| ⑦ | 同じ失敗で無限再生成 | `rescueUsed` は1 submit につき1回。2本目以降の本編呼び出し（fix216/235 の書き直し）は観測だけ | (2)(3) |

### 1-8. 記録

`localStorage['v292Dfix643_log']`（上限40件・古いものから捨てる）

```json
{"ts":"2026-07-29T...","slotId":"chr6","turnIndex":12,"score":9,
 "hits":["repetition-loop","ruby-abuse"],"mode":"shadow","outcome":"hard-observed"}
```

**本文は1バイトも書かない**（テストで固定）。outcome は
`pass` / `hard-observed`(shadow) / `regen-ok` / `regen-hard` / `user-accepted` / `skip-unmeasurable` / `hard-secondary`。

`hard-observed` は仕様書の4値に無い。shadow では救済も停止もしないので `regen-*` と書くのは嘘になるため、
「hard だったが shadow なので何もしなかった」を表す値を足した。

---

## 2. fix644 — fix640 採取の厳格化

### 2-1. 何が入っていたか / なぜ入ったか（真因）

| 実データの候補 | 経路 | 真因 |
|---|---|---|
| 「をかざしながら宿の主人」 | 役割語抽出 | 修飾部の文字クラスに**ひらがなを入れていた**ため、`([一-龥ぁ-んァ-ヶー]{1,8})の(主人…)` が「手をかざしながら宿」の後半8文字を掴んだ |
| 「パチリと」「ポケット」 | §8 カタカナ名抽出 | `extractKatakanaNames` は語形だけ見る。人物として使われたかを誰も見ていなかった |
| 「去年の客」 | 役割語抽出 | 同上（修飾部が時間名詞） |

### 2-2. 直し方（GPT裁定の原則）

**「文字列の見た目ではなく、人物として使われた構文を必須にする」**

- カタカナだから弾く方式は**禁止**（カエデ／ノア／ヒナが消える）
- 助詞を**文字列内の部分一致**で弾く方式も**禁止**（「加賀」が「が」で落ちる）
- 判定は候補の**前後境界と構文**で行う

### 2-3. 実装（fix640 内・純関数）

**`classifyCandidate(s, {declared})` → `{type: 'name'|'role-label'|null, reason}`**

1. 長さ 2〜20（`declared` なら下限1。`<say who="澪">` の1文字名を消さないため）
2. 改行・句読点・引用符・タグ記号を含まない
3. 先頭が `を/に/へ/で/と/から` でない（★**先頭だけ**。内部の助詞は見ない）
4. 活用断片（`ながら|つつ|ている|していた|…`）を含まない
5. 役割語なら `role-label`（別枠）
6. 文節が2つ以上なら `null`。判定は **「内容語＋ひらがな助詞＋内容語」という境界の形**
   - 「加賀」= 漢字が連なるだけ → 当たらない ✅
   - 「山田はな」= 『は』の後ろがひらがな＝境界でない → 当たらない ✅
   - 「宿の主人」= 当たるが 5 で先に role-label になる ✅

**`personUse(prose, name)`** — 地の文から拾う弱い証拠（prose_name / recall）の必須条件

| 型 | 例 |
|---|---|
| `名前 + は/が/も` | カエデは／ノアが |
| `名前 + の + 人の部位・関係語` | ノアの声 |
| `名前 + 敬称` | 佐伯トメさん |
| `名前 + と + 発話動詞` | 「…」とヒナと答えた |
| `名前 + を/に/へ + 人へ向かう動詞` | 加賀を見た／ノアに尋ねた |

「パチリと」「ポケットに手を入れた」はどれにも当たらない → 除外。**カタカナかどうかは1度も見ていない。**

**役割語の修飾部** を `[一-龥々〆ヵ-ヺーA-Za-z…]{1,8}`（ひらがなを含まない内容語）に限定。
これで「手をかざしながら宿の主人を見上げた」から、ゴミではなく **「宿の主人」** が採れる（実測）。

### 2-4. 台帳のフィールド追加（後方互換）

- `candidateType: 'name' | 'role-label'` を追加。`roleWord` は**残す**（fix641 が読んでいる）
- `confidence`（0〜1）を追加。**昇格判定には使わない**（テストで固定）
- 旧台帳（このフィールドが無い）は捨てずに `load()` で補完する。履歴を消さない

### 2-5. 昇格条件は変えていない

`MIN_TURNS = 2` / `MIN_STRONG_KINDS = 2`。`seen>=3` への緩和はしていない。
fix641 のソースに対して直接テストで固定した（`test_fix640.cjs` (14)）。

### 2-6. 実データ実例の扱い（全部テストにした）

| 候補 | 結果 |
|---|---|
| 佐伯トメ | 採取・`name` |
| カエデ / ノア / ヒナ | 採取・`name` |
| 加賀 | 採取・`name`（`が` を含むことは理由にならない・prose_name まで通る） |
| 宿の主人 | 採取・`role-label` |
| をかざしながら宿の主人 | **不採取**（reason=`leading-particle`）。同じ文から「宿の主人」は採れる |
| パチリ / パチリと | **不採取**（人物構文なし） |
| ポケット | **不採取**（人物構文なし） |
| 去年の客 | `name` にはしない |

「§8 が『パチリ』『ポケット』を候補として出していること」自体もテストで確認している
（門番が効いているのか、そもそも候補が来ていないのかを区別するため）。

---

## 3. 変更ファイル

| ファイル | 変更 |
|---|---|
| `v292Dfix643-collapse-rescue.js` | **新規** |
| `v292Dfix640-cast-evidence-ledger.js` | fix644 の形状条件・人物構文・candidateType/confidence・落とした理由の統計 |
| `index.html` | fix643 の script タグ追加（fix641 の後・`?cb=fix643`）／fix640 の `?cb` を `fix640`→`fix644` |
| `test_fix643.cjs` | **新規**（105件） |
| `test_fix640.cjs` | fix644 の (10)〜(14) を追加（87→148件） |
| `test_fix547.cjs` | `__chronicleGetState` 台帳の件数を 33→34（fix643 の分・理由を併記） |

`features.js` / `version.txt` / BUILT / `v292Dfix624` / `v292Dfix641` は**未変更**。

---

## 4. 実機での確認手順（shadow のまま）

```js
// 1) 生きているか
window.__v292Dfix643.status()
//   → { off:false, mode:'shadow', wrapped:true, submitWrapped:true, probe:true, turns:N, ... }
window.__v292Dfix643.selfTest()
//   → { ok:true, repLoopHard:true, registerHard:true, normalAPass:true, streamNotHard:true, ... }

// 2) DO/SAY/STORY を数ターン回してから
window.__v292Dfix643.log()
// 正常ターンなら:
//   {ts, slotId:'…', turnIndex:12, score:0〜3, hits:[], mode:'shadow', outcome:'pass'}
// 崩れたターンが出たら:
//   {…, score:9, hits:['repetition-loop','ruby-abuse'], mode:'shadow', outcome:'hard-observed'}
// 本文が取り出せなかったとき:
//   {…, score:null, hits:[], outcome:'skip-unmeasurable'}

// 3) fix624 の事後判定と突き合わせる（分母が違うので1〜2点ずれる想定）
window.__v292Dfix624.sweep()

// 4) 採取の方
window.__v292Dfix640.report()      // entries[].candidateType / confidence が出る
window.__v292Dfix640.report().stats.dropped   // 何をなぜ採らなかったか
window.__v292Dfix640.why('宿の主人')          // candidateType:'role-label'
```

**shadow で数ターン回したとき `v292Dfix643_log` に入るもの**: 1ターンにつき1行。
ほとんどは `outcome:'pass'` / `score` 0〜3 / `hits` 空。崩壊ターンだけ `hard-observed` が混ざる。
**この分布（何ターンに1回 hard が出るか・偽陽性が混ざっていないか）を見てから live へ上げる。**

実弾に上げるとき: `localStorage.setItem('v292Dfix643Live','1')`（検証端末のみ）
止めるとき: `localStorage.setItem('v292Dfix643Off','1')`

---

## 5. やらなかったこと

- **デプロイ・push・BUILT/version.txt の更新**（禁止指示のとおり）
- **shadow → live の既定切り替え**。実機の分布を見るまで上げない
- **`dash-abuse` の検出器の新設**。fix624 に対応する hit が無い。コードの写像表に席だけ用意した
- **fix627/628 が測っている自己反復・ターン間反復を点数へ入れること**。fix624 が「まだ点数に入れない」と
  明記しているので、閾値を勝手に決めなかった
- **fix624 の閾値・判定ロジックの変更**。判定の正本は1つに保った
- **fix641 の昇格条件の緩和**（GPT再確認どおり据え置き）
- **役割語 → 正式名の自動解決の強化**。fix640 の既存挙動のまま
- **fix482 / fix80 との再試行予算の統合**。救済生成は独立に1回。1ターンの物理送信上限を
  横断的に管理する層は別問題（fix504 のコメントが指摘している射程）。live へ上げる前に再検討が要る **要検証**
