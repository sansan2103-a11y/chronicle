# 設計 fix648 — 句読点隣接重複 collapse（地の文の生成出口正規化）

2026-07-29 / v292-rebuild / 実装: `v292Dfix648-punct-collapse.js` + `test_fix648.cjs`
（BUILT=20260729-fix647 のまま。version.txt は触らない。デプロイ・push 禁止）

## 1. 背景（実データ確定）

全12物語を走査した結果、画面に「、。」「。、」が並ぶ隣接重複は **2件だけ**:

- smr8p8wfr8b T15「それとも、。」
- smrisv41ho7 T12「崩れた。、いや」

どちらも **本文(narrative)側**。カード(`<say>` / `_convSays[].say`)側は **0**。
モデルが文を書きかけて途切れた生出力の名残と見られる。低頻度・cosmetic だが読者の目に触れる。

## 2. GPT裁定の要約（厳守した点）

| 項目 | 裁定 | 本実装での担保 |
|---|---|---|
| 方式 | **案A のみ**（新ターンの地の文を生成出口で正規化） | S.save ラップで確定 narrative を正規化 |
| collapse 対象 | **厳密に2種**: 「、。」→「。」/「。、」→「。」。他は一切触らない | `collapseNarrativePunctuation` は2置換のみ。test(4)(13i) で固定 |
| 一般化 | 「連続句読点を1文字へ」は**禁止** | 「、、」「。。」「……。」「！？」「、！」不変。test(4)。「、。、。」→「。。」で過剰collapseしないことを test(3c) で固定 |
| 非遡及 | 既存保存データを書き換えない。既存2件はそのまま残す | sawGen + baseline + length成長 + `__f648`マーク。test(8) |
| 案B(レンダー時補正) | **却下**（evidenceSpan照合/fix606話者監査/package hash/差分・undo・診断/補正器のDOM読取 が表示と保存の食い違いで壊れる） | 保存される `turn.narrative` そのものを正規化＝表示・保存・push が同一確定値を見る |
| sys 追加 | しない | keeper 登録なし。sys 経路に一切触れない |
| 冪等 | 二度かけて同じ。連鎖「、。、」も残さない | 変化が無くなるまで反復。test(3)(5)(11b) |
| 所有者 | **1か所だけ**（fix555内部やfix427へ二重に入れない） | collapse は fix648 の S.save ラップ1点のみ |
| 記録 | collapse したターンを soft signal として記録（件数/slot/turnIndex）。単独で再生成しない・fix643スコアへ影響させない | `v292Dfix648_log`。再生成経路を一切呼ばない。test(10) |

## 3. 選んだ最終出口フックと根拠

### フック位置 = `S.save` ラップ（`turn.narrative` を正規化）

現物 index.html の submit 末尾（1984〜1996）の順序:

```
1987  const turn = { ..., narrative: narr, plan, _convSays, ... }   ← 地の文が確定
1993  S.turns.push(turn)
1995  S.save()              ← ★ここをラップ。元 save(保存・cloud push)へ委譲する前に正規化
1996  UI.appendTurn(turn)   ← 正規化後の turn.narrative を読んで描画する
```

**なぜここが「すべての句読点編集の後・保存/表示/push前」なのか:**

1. **すべての句読点編集の後** — 句読点を編集する唯一の実処理は fix555（Api.call 直後の校正）で、
   これは parsePlan よりさらに前に確定する。say-tag 除去（1966-1982）も turn 組み立て前に済む。
   よって S.save 到達時点で narrative の句読点編集はすべて完了している。fix553 は診断のみ（編集しない）。
2. **保存前** — 元の `S.save()`（永続化・fix399 cloud push）へ委譲する **前** に `turn.narrative` を書き換える。
3. **表示前** — `UI.appendTurn` は `S.save()` の **後** に呼ばれ、正規化済みの `turn.narrative` を読む。
   さらに fix645 の `UI.renderNarr` 二重ネットも同じ正規化済み文字列を受け取る＝表示と保存が一致。
4. **push前** — cloud push は元 save の内側なので、正規化済みの本文が上がる。
5. **構造化後の地の文ノードだけ** — raw 文字列全体へ regex をかけず、確定した `turn.narrative`
   （＝画面本文になる地の文フィールド）1つだけに適用。`<say>`（本文からは既に除去済み）・
   `_convSays[].say`（会話カード）・`plan.narrative`（話者タグ付き一次証拠配列）には触れない。

前例に倣った点: **fix645** が同じ `S.save` ラップ位置で `turn.narrative` から scene_move タグを剥がし
（「保存・cloud送出・表示のすべてより前の1点」＝fix427 A2 と同じ）、**fix427** も S.save 直前で
`_convSays` を補正している。fix648 はこの実績ある1点に相乗りする。

### fix555 との順序

```
モデル生出力
  → fix555 句読点校正（Api.call ラップ・parsePlan の前）        ← 句読点を実際に編集する唯一の処理
  → parsePlan / say-tag 除去 / turn 組み立て
  → fix553 診断（編集しない）
  → ★fix648 地の文正規化（S.save ラップ・元 save へ委譲する前）  ← 本実装
  → 保存・表示・push
```

fix555 は Api.call の戻り値を直接書き換える（`r.text = newBody + tail`）。その結果が parsePlan を通り
`narr` になり `turn.narrative` へ入る。fix648 はそのさらに後段（S.save）で走るので、
**fix555 の校正結果を必ず入力として受け取る**。両者が同じ本文を二重編集する競合はない
（fix555=無句読点長文への句読点“追加”、fix648=隣接“重複”の collapse。対象がそもそも異なる）。

### fix645 との多重ラップ順序

ロード順で fix648 を fix645 の後ろに置くため、fix648 が `S.save` の **最外ラップ**になり、
fix648 の正規化 → fix645 の scene_move 剥がし → 元 save の順で走る。
collapse は「、。」「。、」しか触らず `<scene_move .../>` タグには影響しないので、
fix645 の剥がし・fix427 の補正と**直交**し順序競合しない（test(12) で実チェーン確認）。
両ラッパとも自マーカー（`S.__f648save` / `S.__f645save`）で二重ラップを防ぐ。

## 4. 非遡及の担保（既存2件を書き換えない）

fix645 と同型の二段ガード + 追加の baseline:

1. **sawGen** — parsePlan が走った（＝このセッションで実際に生成した）ことを確認。
   履歴ロードだけの save では false なので何もしない。
2. **baseline（lastLen）** — parsePlan は「新ターンを push する前」に走る。この瞬間の
   `S.turns.length` を1度だけ baseline にする（＝生成開始前から在ったターン数）。
   以後 length がこれを超えたぶん（=新ターン）だけ処理する。baseline を取らず lastLen=-1 のままだと、
   直後の save で末尾（既存ターン）を誤って書き換えうる（この穴は test(8d) が検出したので塞いだ）。
3. **`__f648` マーク** — 同じターンを二度処理しない（冪等）。

対象は常に「length が成長したときの末尾1ターン」だけ。既存の途中ターン（T12/T15）は末尾でも
新規でもないので触れない。

## 5. 純関数（冪等・厳密2種）

```js
function collapseNarrativePunctuation(text){
  var s = String(text==null?'':text), prev;
  do { prev = s; s = s.split('、。').join('。').split('。、').join('。'); } while (s !== prev);
  return s;
}
```

- GPT提示の関数（replaceAll 版）とほぼ同一。ただし1パスでは「、。、」等の連鎖が残るので
  **変化が無くなるまで反復**して冪等化。`replaceAll` 非対応エンジンでも動くよう split/join を使用。
- 反復しても触る文字列は「、。」「、。」の2種のみ。「、、」「。。」等を新たに対象化することはない。
  例:「、。、。」→「。。」（別々の2組は別々の「。」として残る＝過剰 collapse しない）。

## 6. 記録（soft signal・観測だけ）

`v292Dfix648_log`（上限100件）へ `{ts, slotId, turnIndex, collapses}` を記録。本文は保存しない。
**再生成しない・fix643 のスコアへ影響させない**（このファイルは再生成経路を一切呼ばない。test(10g)）。
読出: `window.__v292Dfix648.stats()`（`turnsCollapsed` = fix648Applied 相当 / `collapses` = 置換総数）。

## 7. 規約遵守

- 新規 `v292Dfix648-punct-collapse.js`。冪等ガード `window.__v292Dfix648.__armed`・OFF `v292Dfix648Off`。
  ラップ方式（features.js を直接編集しない）。
- index.html へ Python byte 挿入で `<script src="...?cb=fix648">` を fix645 の直後・`</body>` 前に追加。
  NUL 1個を保持（挿入後も NUL count=1）。BUILT / version.txt は触らない。
- `test_fix648.cjs` は実チェーン方式（配信物 fix648 を mock window 上でそのまま実行）。
  BUILT/cb/件数を固定値で縛らず、契約（正規表現・入出力）で縛る。
